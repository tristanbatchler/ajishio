[project]
name = "ajishio-demo-projects"
version = "0.1.0"
description = "Example games demonstrating how to use the Ajishio 2D game engine."
requires-python = ">=3.14"
dependencies = [
    "ajishio",
]

