{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Ajishio","text":"<p>A fun, GameMaker-inspired 2D game engine built on pygame-ce for making unserious games quickly.</p> <p>The API reference page is generated from the public <code>ajishio</code> namespace exports, not maintained manually.</p> <pre><code>import ajishio as aj\n\nclass Player(aj.GameObject):\n    def step(self):\n        if aj.keyboard_check(aj.vk_right):\n            self.x += 4 * aj.delta_time\n\naj.register_objects(Player)\naj.set_rooms(levels)\naj.game_start()\n</code></pre> <p>See the API Reference for full details.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#api-reference","title":"API Reference","text":"<p>This page documents the public <code>aj.</code> namespace. All functions and classes listed here are available directly from <code>ajishio</code>.</p>"},{"location":"api/#game-objects","title":"Game Objects","text":""},{"location":"api/#ajishio.game_object.GameObject","title":"ajishio.game_object.GameObject","text":"<p>               Bases: <code>IGameObject</code></p>"},{"location":"api/#ajishio.game_object.GameObject.place_meeting","title":"place_meeting","text":"<pre><code>place_meeting(\n    x: float,\n    y: float,\n    obj: IGameObject | type[IGameObject] | UUID,\n) -&gt; IGameObject | None\n</code></pre> <p>With this function you can check a position for a collision with another instance or all instances of an object using the collision mask of the instance that runs the code for the check. When you use this you are effectively asking Ajishio to move the instance to the new position, check for a collision, move back and tell you if a collision was found or not.</p> <p>This function will return the unique instance id of the object being collided. This function will return <code>None</code> if no collision occurs, or the exact instance found if a collision does occur.</p>"},{"location":"api/#engine-control","title":"Engine Control","text":""},{"location":"api/#ajishio.engine.Engine.audio_is_playing","title":"ajishio.engine.Engine.audio_is_playing","text":"<pre><code>audio_is_playing(index: GameSound) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.audio_play_sound","title":"ajishio.engine.Engine.audio_play_sound","text":"<pre><code>audio_play_sound(\n    index: GameSound, loop: bool = False, gain: float = 1\n) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.collision_rectangle","title":"ajishio.engine.Engine.collision_rectangle","text":"<pre><code>collision_rectangle(\n    x1: float,\n    y1: float,\n    x2: float,\n    y2: float,\n    obj: IGameObject | type[IGameObject] | UUID,\n    not_these: Container[IGameObject] | None = None,\n) -&gt; IGameObject | None\n</code></pre> <p>This function checks a rectangular area for a collision with the given instance or instances of the given object, and returns the first result found.</p> <p>You may also choose to supply any special instances that are excluded from collision checks.</p> <p>This function will return the unique id of the instance being collided with. If no collisions are found, noone is returned.</p>"},{"location":"api/#ajishio.engine.Engine.collision_rectangle_list","title":"ajishio.engine.Engine.collision_rectangle_list","text":"<pre><code>collision_rectangle_list(\n    left: float, top: float, right: float, bottom: float\n) -&gt; list[IGameObject]\n</code></pre> <p>This function checks a rectangular area for a collision with all instances. It returns the list of all instances in the supplied rectangular region.</p> <p>This function is the same as the collision_rectangle() function, only instead of just detecting one instance at a time, it will detect multiple instances. You supply the x/y positions of the top left and bottom right of the area to check.</p>"},{"location":"api/#ajishio.engine.Engine.game_end","title":"ajishio.engine.Engine.game_end","text":"<pre><code>game_end() -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.game_restart","title":"ajishio.engine.Engine.game_restart","text":"<pre><code>game_restart() -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.game_set_speed","title":"ajishio.engine.Engine.game_set_speed","text":"<pre><code>game_set_speed(speed: float) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.game_start","title":"ajishio.engine.Engine.game_start","text":"<pre><code>game_start() -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.game_start_async","title":"ajishio.engine.Engine.game_start_async  <code>async</code>","text":"<pre><code>game_start_async() -&gt; None\n</code></pre> <p>Async version of game_start for pygbag/WASM compatibility. Yields control to the browser event loop every frame.</p>"},{"location":"api/#ajishio.engine.Engine.instance_count","title":"ajishio.engine.Engine.instance_count","text":"<pre><code>instance_count(obj: type[IGameObject]) -&gt; int\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.instance_destroy","title":"ajishio.engine.Engine.instance_destroy","text":"<pre><code>instance_destroy(obj: IGameObject) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.instance_exists","title":"ajishio.engine.Engine.instance_exists","text":"<pre><code>instance_exists(obj: type[IGameObject]) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.instance_find","title":"ajishio.engine.Engine.instance_find","text":"<pre><code>instance_find(\n    obj: type[IGameObject] | str, n: int = 0\n) -&gt; IGameObject | None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.instance_position","title":"ajishio.engine.Engine.instance_position","text":"<pre><code>instance_position(\n    x: float,\n    y: float,\n    obj: IGameObject | type[IGameObject] | str,\n) -&gt; IGameObject | None\n</code></pre> <p>With this function you can check a position for a collision with another instance or all instances of an object. When you use this you are checking a single point in the room for an instance or an object. This check will be done against the collision mask of the instance and will return the unique instance id. This function will return <code>None</code> if no collision occurs, or the exact instance found if a collision does occur.</p>"},{"location":"api/#ajishio.engine.Engine.instances_iterate","title":"ajishio.engine.Engine.instances_iterate","text":"<pre><code>instances_iterate(\n    obj: type[IGameObject],\n) -&gt; Iterable[IGameObject]\n</code></pre> <p>With this function you can iterate over all instances of an object. This is especially useful for doing something to all instances of an object in a loop.</p>"},{"location":"api/#ajishio.engine.Engine.register_objects","title":"ajishio.engine.Engine.register_objects","text":"<pre><code>register_objects(*objects: type[IGameObject]) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_goto","title":"ajishio.engine.Engine.room_goto","text":"<pre><code>room_goto(index: int) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_goto_next","title":"ajishio.engine.Engine.room_goto_next","text":"<pre><code>room_goto_next() -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_goto_previous","title":"ajishio.engine.Engine.room_goto_previous","text":"<pre><code>room_goto_previous() -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_restart","title":"ajishio.engine.Engine.room_restart","text":"<pre><code>room_restart() -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_set_background","title":"ajishio.engine.Engine.room_set_background","text":"<pre><code>room_set_background(color: Color) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_set_height","title":"ajishio.engine.Engine.room_set_height","text":"<pre><code>room_set_height(h: int) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_set_size","title":"ajishio.engine.Engine.room_set_size","text":"<pre><code>room_set_size(w: float, h: float) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.room_set_width","title":"ajishio.engine.Engine.room_set_width","text":"<pre><code>room_set_width(w: int) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.set_rooms","title":"ajishio.engine.Engine.set_rooms","text":"<pre><code>set_rooms(rooms: list[GameLevel]) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.view_set_hport","title":"ajishio.engine.Engine.view_set_hport","text":"<pre><code>view_set_hport(view_idx: int, h: float) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.view_set_wport","title":"ajishio.engine.Engine.view_set_wport","text":"<pre><code>view_set_wport(view_idx: int, w: float) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.view_set_xport","title":"ajishio.engine.Engine.view_set_xport","text":"<pre><code>view_set_xport(view_idx: int, x: float) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.view_set_yport","title":"ajishio.engine.Engine.view_set_yport","text":"<pre><code>view_set_yport(view_idx: int, y: float) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.engine.Engine.window_set_size","title":"ajishio.engine.Engine.window_set_size","text":"<pre><code>window_set_size(w: int, h: int) -&gt; None\n</code></pre>"},{"location":"api/#rendering","title":"Rendering","text":""},{"location":"api/#ajishio.rendering.Renderer.draw_circle","title":"ajishio.rendering.Renderer.draw_circle","text":"<pre><code>draw_circle(\n    x: float,\n    y: float,\n    r: float,\n    outline: bool = False,\n    color: Color | None = None,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre> <p>With this function you can draw either an outline of a circle or a filled circle. You can define how precise the drawing is with the function <code>draw_set_circle_precision()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>float</code> <p>The x coordinate of the center of the circle.</p> required <code>y</code> <code>float</code> <p>The y coordinate of the center of the circle.</p> required <code>r</code> <code>float</code> <p>The circle's radius (length from its center to its edge)</p> required <code>outline</code> <code>bool</code> <p>Whether the circle is drawn filled (false) or as a one pixel wide outline (true).</p> <code>False</code> <code>color</code> <code>Color | None</code> <p>The color to draw with. If None, uses the current draw color.</p> <code>None</code> <code>alpha</code> <code>float</code> <p>The opacity of the circle, between 0.0 (fully transparent) and 1.0 (fully opaque).</p> <code>1.0</code>"},{"location":"api/#ajishio.rendering.Renderer.draw_ellipse","title":"ajishio.rendering.Renderer.draw_ellipse","text":"<pre><code>draw_ellipse(\n    x1: float,\n    y1: float,\n    x2: float,\n    y2: float,\n    outline: bool = False,\n    color: Color | None = None,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre> <p>With this function you can draw either an outline of an ellipse or a filled ellipse by defining a rectangular area that will then have the ellipse created to fit. You can define how precise the drawing is with the function draw_set_circle_precision().</p> <p>Parameters:</p> Name Type Description Default <code>x1</code> <code>float</code> <p>The x coordinate of the left of the ellipse.</p> required <code>y1</code> <code>float</code> <p>The y coordinate of the top of the ellipse.</p> required <code>x2</code> <code>float</code> <p>The x coordinate of the right of the ellipse.</p> required <code>y2</code> <code>float</code> <p>The y coordinate of the bottom of the ellipse.</p> required <code>outline</code> <code>bool</code> <p>Whether the ellipse is drawn filled (false) or as a one pixel wide outline (true).</p> <code>False</code> <code>color</code> <code>Color | None</code> <p>The color to draw with. If None, uses the current draw color.</p> <code>None</code> <code>alpha</code> <code>float</code> <p>The opacity of the ellipse, between 0.0 (fully transparent) and 1.0 (fully opaque).</p> <code>1.0</code>"},{"location":"api/#ajishio.rendering.Renderer.draw_line","title":"ajishio.rendering.Renderer.draw_line","text":"<pre><code>draw_line(\n    x1: float,\n    y1: float,\n    x2: float,\n    y2: float,\n    color: Color | None = None,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.draw_point","title":"ajishio.rendering.Renderer.draw_point","text":"<pre><code>draw_point(\n    x: float,\n    y: float,\n    color: Color | None = None,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.draw_rectangle","title":"ajishio.rendering.Renderer.draw_rectangle","text":"<pre><code>draw_rectangle(\n    x1: float,\n    y1: float,\n    x2: float,\n    y2: float,\n    outline: bool = False,\n    color: Color | None = None,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre> <p>This function draws either an outline of a rectangle or a filled rectangle where the (x1,y1) position is the top left corner and the (x2,y2) position is the bottom right corner.</p> <p>Parameters:</p> Name Type Description Default <code>x1</code> <code>float</code> <p>The x coordinate of the top left corner of the rectangle.</p> required <code>y1</code> <code>float</code> <p>The y coordinate of the top left corner of the rectangle.</p> required <code>x2</code> <code>float</code> <p>The x coordinate of the bottom right corner of the rectangle.</p> required <code>y2</code> <code>float</code> <p>The y coordinate of the bottom right corner of the rectangle.</p> required <code>outline</code> <code>bool</code> <p>Whether the rectangle is drawn filled (false) or as a one pixel wide outline (true).</p> <code>False</code> <code>color</code> <code>Color | None</code> <p>The color to draw with. If None, uses the current draw color.</p> <code>None</code> <code>alpha</code> <code>float</code> <p>The opacity of the rectangle, between 0.0 (fully transparent) and 1.0 (fully opaque).</p> <code>1.0</code>"},{"location":"api/#ajishio.rendering.Renderer.draw_set_font","title":"ajishio.rendering.Renderer.draw_set_font","text":"<pre><code>draw_set_font(\n    font: Font, fallbacks: Iterable[Font] | None = None\n) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.draw_sprite","title":"ajishio.rendering.Renderer.draw_sprite","text":"<pre><code>draw_sprite(\n    x: float,\n    y: float,\n    sprite_index: GameSprite,\n    image_index: int,\n    x_scale: float = 1.0,\n    y_scale: float = 1.0,\n    rotation: float = 0.0,\n    color: Color = c_white,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.draw_text","title":"ajishio.rendering.Renderer.draw_text","text":"<pre><code>draw_text(\n    x: float,\n    y: float,\n    string: str,\n    color: Color | None = None,\n) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.draw_triangle","title":"ajishio.rendering.Renderer.draw_triangle","text":"<pre><code>draw_triangle(\n    x1: float,\n    y1: float,\n    x2: float,\n    y2: float,\n    x3: float,\n    y3: float,\n    col1: Color | None = None,\n    col2: Color | None = None,\n    col3: Color | None = None,\n    outline: bool = False,\n    alpha: float = 1.0,\n) -&gt; None\n</code></pre> <p>With this function you can draw either an outline of a triangle or a filled triangle. If it is filled you can define the individual colours for each corner point and if these colours are not the same, you will get a gradient effect from one to the other.</p>"},{"location":"api/#ajishio.rendering.load_font","title":"ajishio.rendering.load_font","text":"<pre><code>load_font(font_path: Path | str, size: int) -&gt; pg.font.Font\n</code></pre>"},{"location":"api/#ajishio.rendering.make_color_hsv","title":"ajishio.rendering.make_color_hsv","text":"<pre><code>make_color_hsv(hue: float, sat: float, val: float) -&gt; Color\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.text_height","title":"ajishio.rendering.Renderer.text_height","text":"<pre><code>text_height(string: str) -&gt; int\n</code></pre>"},{"location":"api/#ajishio.rendering.Renderer.text_width","title":"ajishio.rendering.Renderer.text_width","text":"<pre><code>text_width(string: str) -&gt; int\n</code></pre>"},{"location":"api/#input","title":"Input","text":""},{"location":"api/#ajishio.input.keyboard_check","title":"ajishio.input.keyboard_check","text":"<pre><code>keyboard_check(key: int) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.keyboard_check_pressed","title":"ajishio.input.keyboard_check_pressed","text":"<pre><code>keyboard_check_pressed(key: int) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.keyboard_check_released","title":"ajishio.input.keyboard_check_released","text":"<pre><code>keyboard_check_released(key: int) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.mouse_check_button","title":"ajishio.input.mouse_check_button","text":"<pre><code>mouse_check_button(mb: int) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.mouse_check_button_pressed","title":"ajishio.input.mouse_check_button_pressed","text":"<pre><code>mouse_check_button_pressed(mb: int) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.mouse_check_button_released","title":"ajishio.input.mouse_check_button_released","text":"<pre><code>mouse_check_button_released(mb: int) -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.mouse_wheel_down","title":"ajishio.input.mouse_wheel_down","text":"<pre><code>mouse_wheel_down() -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.mouse_wheel_up","title":"ajishio.input.mouse_wheel_up","text":"<pre><code>mouse_wheel_up() -&gt; bool\n</code></pre>"},{"location":"api/#ajishio.input.ord","title":"ajishio.input.ord","text":"<pre><code>ord(char: str) -&gt; int\n</code></pre>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#ajishio.utils.clamp","title":"ajishio.utils.clamp","text":"<pre><code>clamp(value: float, min: float, max: float) -&gt; float\n</code></pre>"},{"location":"api/#ajishio.utils.lengthdir_x","title":"ajishio.utils.lengthdir_x","text":"<pre><code>lengthdir_x(length: float, direction: float) -&gt; float\n</code></pre> <p>Returns the length of the x component of a vector with the given length and direction (in radians).</p>"},{"location":"api/#ajishio.utils.lengthdir_y","title":"ajishio.utils.lengthdir_y","text":"<pre><code>lengthdir_y(length: float, direction: float) -&gt; float\n</code></pre> <p>Returns the length of the y component of a vector with the given length and direction (in radians).</p>"},{"location":"api/#ajishio.utils.lerp","title":"ajishio.utils.lerp","text":"<pre><code>lerp(start: float, end: float, t: float) -&gt; float\n</code></pre>"},{"location":"api/#ajishio.utils.map_value","title":"ajishio.utils.map_value","text":"<pre><code>map_value(\n    value: float,\n    min: float,\n    max: float,\n    new_min: float,\n    new_max: float,\n) -&gt; float\n</code></pre>"},{"location":"api/#ajishio.utils.point_distance","title":"ajishio.utils.point_distance","text":"<pre><code>point_distance(\n    x1: float, y1: float, x2: float, y2: float\n) -&gt; float\n</code></pre>"},{"location":"api/#ajishio.utils.profile","title":"ajishio.utils.profile","text":"<pre><code>profile(fn: Callable[_P, _R]) -&gt; Callable[_P, _R]\n</code></pre> <p>Decorator that profiles fn when <code>--profile</code> is passed on the command line.</p> <p>The <code>.prof</code> file is saved to the current working directory and named after the module that owns the decorated function (e.g. <code>platformer.prof</code>). Without <code>--profile</code> the function runs completely unmodified.</p> <p>Example::</p> <pre><code>@aj.profile\ndef main() -&gt; None:\n    aj.game_start()\n\nmain()\n</code></pre>"},{"location":"api/#ajishio.utils.room_set_caption","title":"ajishio.utils.room_set_caption","text":"<pre><code>room_set_caption(caption: str) -&gt; None\n</code></pre>"},{"location":"api/#ajishio.utils.sign","title":"ajishio.utils.sign","text":"<pre><code>sign(value: float) -&gt; int\n</code></pre>"},{"location":"api/#types-constants","title":"Types &amp; Constants","text":""},{"location":"api/#ajishio.types.CollisionMask","title":"ajishio.types.CollisionMask  <code>dataclass</code>","text":""},{"location":"api/#ajishio.CustomFields","title":"ajishio.CustomFields","text":"<pre><code>CustomFields = dict[str, object]\n</code></pre>"},{"location":"api/#ajishio.types.Entity","title":"ajishio.types.Entity","text":"<p>               Bases: <code>TypedDict</code></p>"},{"location":"api/#ajishio.types.GameLevel","title":"ajishio.types.GameLevel  <code>dataclass</code>","text":""},{"location":"api/#ajishio.types.GameObjectKwargs","title":"ajishio.types.GameObjectKwargs","text":"<p>               Bases: <code>TypedDict</code></p>"},{"location":"api/#ajishio.types.GameSprite","title":"ajishio.types.GameSprite  <code>dataclass</code>","text":""},{"location":"api/#ajishio.types.IGameObject","title":"ajishio.types.IGameObject","text":"<p>               Bases: <code>Protocol</code></p>"},{"location":"api/#ajishio.types.IGameObject.on_destroy","title":"on_destroy","text":"<pre><code>on_destroy() -&gt; None\n</code></pre> <p>This event is the event to be executed when an instance is destroyed. It is often overlooked when adding behaviours to objects, but it can be very useful, for example by creating explosion or particle effects when an enemy is killed, or for respawning a new instance of the object in another part of the room, or even for adding points to a score.</p>"},{"location":"api/#asset-loaders","title":"Asset Loaders","text":""},{"location":"api/#ajishio.sprite_loader.load_aseprite_sprite","title":"ajishio.sprite_loader.load_aseprite_sprite","text":"<pre><code>load_aseprite_sprite(sprite_dir: Path) -&gt; GameSprite\n</code></pre>"},{"location":"api/#ajishio.sprite_loader.load_aseprite_sprites","title":"ajishio.sprite_loader.load_aseprite_sprites","text":"<pre><code>load_aseprite_sprites(\n    sprites_directory: Path,\n) -&gt; dict[str, GameSprite]\n</code></pre>"},{"location":"api/#ajishio.sprite_loader.load_sprite_from_sheet","title":"ajishio.sprite_loader.load_sprite_from_sheet","text":"<pre><code>load_sprite_from_sheet(\n    sheet_path: Path,\n    width: int,\n    height: int,\n    padding: int = 0,\n    columns: int = 1,\n    rows: int = 1,\n    offset_x: int = 0,\n    offset_y: int = 0,\n) -&gt; GameSprite\n</code></pre>"},{"location":"api/#ajishio.sprite_loader.sprite_set_offset","title":"ajishio.sprite_loader.sprite_set_offset","text":"<pre><code>sprite_set_offset(\n    sprite: GameSprite, x_offset: float, y_offset: float\n) -&gt; None\n</code></pre>"},{"location":"api/#asset-loaders_1","title":"Asset Loaders","text":""},{"location":"api/#ajishio.sound_loader.load_sound","title":"ajishio.sound_loader.load_sound","text":"<pre><code>load_sound(sound_file: Path) -&gt; GameSound\n</code></pre>"},{"location":"api/#ajishio.sound_loader.load_sounds","title":"ajishio.sound_loader.load_sounds","text":"<pre><code>load_sounds(sounds_directory: Path) -&gt; dict[str, GameSound]\n</code></pre>"},{"location":"api/#asset-loaders_2","title":"Asset Loaders","text":""},{"location":"api/#ajishio.level_loader.load_ldtk_levels","title":"ajishio.level_loader.load_ldtk_levels","text":"<pre><code>load_ldtk_levels(\n    ldtk_super_simple_export_simplified_path: Path,\n    *,\n    cosmetic_layers: Collection[str] = (),\n) -&gt; list[GameLevel]\n</code></pre> <p>Load all LDtk super-simple levels in a simplified export directory.</p> <p>Parameters:</p> Name Type Description Default <code>ldtk_super_simple_export_simplified_path</code> <code>Path</code> <p>Path to LDtk's <code>simplified/</code> folder.</p> required <code>cosmetic_layers</code> <code>Collection[str]</code> <p>Layer names that should render from <code>.png</code> only and skip object spawning from <code>.csv</code> data.</p> <code>()</code>"},{"location":"api/#audio","title":"Audio","text":""},{"location":"api/#ajishio.game_sound.GameSound","title":"ajishio.game_sound.GameSound","text":""},{"location":"api/#other","title":"Other","text":""},{"location":"api/#pygame.color.Color","title":"pygame.color.Color","text":"<p>Color(r, g, b) -&gt; Color Color(r, g, b, a=255) -&gt; Color Color(color_value) -&gt; Color pygame object for color representations</p>"},{"location":"api/#pygame.color.Color.a","title":"a  <code>property</code>","text":"<pre><code>a: int\n</code></pre> <p>a -&gt; int Gets or sets the alpha value of the Color.</p>"},{"location":"api/#pygame.color.Color.b","title":"b  <code>property</code>","text":"<pre><code>b: int\n</code></pre> <p>b -&gt; int Gets or sets the blue value of the Color.</p>"},{"location":"api/#pygame.color.Color.cmy","title":"cmy  <code>property</code>","text":"<pre><code>cmy: tuple[float, float, float]\n</code></pre> <p>cmy -&gt; tuple Gets or sets the CMY representation of the Color.</p>"},{"location":"api/#pygame.color.Color.g","title":"g  <code>property</code>","text":"<pre><code>g: int\n</code></pre> <p>g -&gt; int Gets or sets the green value of the Color.</p>"},{"location":"api/#pygame.color.Color.hex","title":"hex  <code>property</code>","text":"<pre><code>hex: str\n</code></pre> <p>hex -&gt; str Gets or sets the stringified hexadecimal representation of the Color.</p>"},{"location":"api/#pygame.color.Color.hsla","title":"hsla  <code>property</code>","text":"<pre><code>hsla: tuple[float, float, float, float]\n</code></pre> <p>hsla -&gt; tuple Gets or sets the HSLA representation of the Color.</p>"},{"location":"api/#pygame.color.Color.hsva","title":"hsva  <code>property</code>","text":"<pre><code>hsva: tuple[float, float, float, float]\n</code></pre> <p>hsva -&gt; tuple Gets or sets the HSVA representation of the Color.</p>"},{"location":"api/#pygame.color.Color.i1i2i3","title":"i1i2i3  <code>property</code>","text":"<pre><code>i1i2i3: tuple[float, float, float]\n</code></pre> <p>i1i2i3 -&gt; tuple Gets or sets the I1I2I3 representation of the Color.</p>"},{"location":"api/#pygame.color.Color.normalized","title":"normalized  <code>property</code>","text":"<pre><code>normalized: tuple[float, float, float, float]\n</code></pre> <p>normalized -&gt; tuple Gets or sets the normalized representation of the Color.</p>"},{"location":"api/#pygame.color.Color.r","title":"r  <code>property</code>","text":"<pre><code>r: int\n</code></pre> <p>r -&gt; int Gets or sets the red value of the Color.</p>"},{"location":"api/#pygame.color.Color.correct_gamma","title":"correct_gamma  <code>method descriptor</code>","text":"<pre><code>correct_gamma() -&gt; Color\n</code></pre> <p>correct_gamma(gamma, /) -&gt; Color Applies a certain gamma value to the Color.</p>"},{"location":"api/#pygame.color.Color.from_cmy","title":"from_cmy  <code>builtin</code>","text":"<pre><code>from_cmy(object: tuple[float, float, float]) -&gt; Color\n</code></pre><pre><code>from_cmy(c: float, m: float, y: float) -&gt; Color\n</code></pre> <pre><code>from_cmy() -&gt; Color\n</code></pre> <p>from_cmy(object, /) -&gt; Color from_cmy(c, m, y, /) -&gt; Color Returns a Color object from a CMY representation</p>"},{"location":"api/#pygame.color.Color.from_hex","title":"from_hex  <code>builtin</code>","text":"<pre><code>from_hex() -&gt; Color\n</code></pre> <p>from_hex(hex, /) -&gt; Color Returns a Color object from a Hexadecimal representation</p>"},{"location":"api/#pygame.color.Color.from_hsla","title":"from_hsla  <code>builtin</code>","text":"<pre><code>from_hsla(\n    object: tuple[float, float, float, float],\n) -&gt; Color\n</code></pre><pre><code>from_hsla(h: float, s: float, l: float, a: float) -&gt; Color\n</code></pre> <pre><code>from_hsla() -&gt; Color\n</code></pre> <p>from_hsla(object, /) -&gt; Color from_hsla(h, s, l, a, /) -&gt; Color Returns a Color object from an HSLA representation</p>"},{"location":"api/#pygame.color.Color.from_hsva","title":"from_hsva  <code>builtin</code>","text":"<pre><code>from_hsva(\n    object: tuple[float, float, float, float],\n) -&gt; Color\n</code></pre><pre><code>from_hsva(h: float, s: float, v: float, a: float) -&gt; Color\n</code></pre> <pre><code>from_hsva() -&gt; Color\n</code></pre> <p>from_hsva(object, /) -&gt; Color from_hsva(h, s, v, a, /) -&gt; Color Returns a Color object from an HSVA representation</p>"},{"location":"api/#pygame.color.Color.from_i1i2i3","title":"from_i1i2i3  <code>builtin</code>","text":"<pre><code>from_i1i2i3(object: tuple[float, float, float]) -&gt; Color\n</code></pre><pre><code>from_i1i2i3(i1: float, i2: float, i3: float) -&gt; Color\n</code></pre> <pre><code>from_i1i2i3() -&gt; Color\n</code></pre> <p>from_i1i2i3(object, /) -&gt; Color from_i1i2i3(i1, i2, i3, /) -&gt; Color Returns a Color object from a I1I2I3 representation</p>"},{"location":"api/#pygame.color.Color.from_normalized","title":"from_normalized  <code>builtin</code>","text":"<pre><code>from_normalized(\n    object: tuple[float, float, float, float],\n) -&gt; Color\n</code></pre><pre><code>from_normalized(\n    r: float, g: float, b: float, a: float\n) -&gt; Color\n</code></pre> <pre><code>from_normalized() -&gt; Color\n</code></pre> <p>from_normalized(object, /) -&gt; Color from_normalized(r, g, b, a /) -&gt; Color Returns a Color object from a Normalized representation</p>"},{"location":"api/#pygame.color.Color.grayscale","title":"grayscale  <code>method descriptor</code>","text":"<pre><code>grayscale() -&gt; Color\n</code></pre> <p>grayscale() -&gt; Color returns the grayscale of a Color</p>"},{"location":"api/#pygame.color.Color.lerp","title":"lerp  <code>method descriptor</code>","text":"<pre><code>lerp() -&gt; Color\n</code></pre> <p>lerp(Color, float) -&gt; Color returns a linear interpolation to the given Color.</p>"},{"location":"api/#pygame.color.Color.normalize","title":"normalize  <code>method descriptor</code>","text":"<pre><code>normalize() -&gt; tuple[float, float, float, float]\n</code></pre> <p>normalize() -&gt; tuple Returns the normalized RGBA values of the Color.</p>"},{"location":"api/#pygame.color.Color.premul_alpha","title":"premul_alpha  <code>method descriptor</code>","text":"<pre><code>premul_alpha() -&gt; Color\n</code></pre> <p>premul_alpha() -&gt; Color returns a Color where the r,g,b components have been multiplied by the alpha.</p>"},{"location":"api/#pygame.color.Color.set_length","title":"set_length  <code>method descriptor</code>","text":"<pre><code>set_length() -&gt; None\n</code></pre> <p>set_length(len, /) -&gt; None Set the number of elements in the Color to 1,2,3, or 4.</p>"},{"location":"api/#pygame.color.Color.update","title":"update  <code>method descriptor</code>","text":"<pre><code>update(r: int, g: int, b: int, a: int = 255) -&gt; None\n</code></pre><pre><code>update(rgbvalue: ColorLike) -&gt; None\n</code></pre> <pre><code>update()\n</code></pre> <p>update(r, g, b, /) -&gt; None update(r, g, b, a=255, /) -&gt; None update(color_value, /) -&gt; None Sets the elements of the color</p>"},{"location":"api/#pygame.font.Font","title":"pygame.font.Font","text":"<p>Font(filename=None, size=20) -&gt; Font Font(filename, size) -&gt; Font Font(pathlib.Path, size) -&gt; Font Font(object, size) -&gt; Font create a new Font object from a file</p>"},{"location":"api/#pygame.font.Font.align","title":"align  <code>property</code>","text":"<pre><code>align: int\n</code></pre> <p>align -&gt; int Set how rendered text is aligned when given a wrap length.</p>"},{"location":"api/#pygame.font.Font.bold","title":"bold  <code>property</code>","text":"<pre><code>bold: bool\n</code></pre> <p>bold -&gt; bool Gets or sets whether the font should be rendered in (faked) bold.</p>"},{"location":"api/#pygame.font.Font.italic","title":"italic  <code>property</code>","text":"<pre><code>italic: bool\n</code></pre> <p>italic -&gt; bool Gets or sets whether the font should be rendered in (faked) italics.</p>"},{"location":"api/#pygame.font.Font.name","title":"name  <code>property</code>","text":"<pre><code>name: str\n</code></pre> <p>name -&gt; str Gets the font's name.</p>"},{"location":"api/#pygame.font.Font.point_size","title":"point_size  <code>property</code>","text":"<pre><code>point_size: int\n</code></pre> <p>point_size -&gt; int Gets or sets the font's point size</p>"},{"location":"api/#pygame.font.Font.strikethrough","title":"strikethrough  <code>property</code>","text":"<pre><code>strikethrough: bool\n</code></pre> <p>strikethrough -&gt; bool Gets or sets whether the font should be rendered with a strikethrough.</p>"},{"location":"api/#pygame.font.Font.style_name","title":"style_name  <code>property</code>","text":"<pre><code>style_name: str\n</code></pre> <p>style_name -&gt; str Gets the font's style_name.</p>"},{"location":"api/#pygame.font.Font.underline","title":"underline  <code>property</code>","text":"<pre><code>underline: bool\n</code></pre> <p>underline -&gt; bool Gets or sets whether the font should be rendered with an underline.</p>"},{"location":"api/#pygame.font.Font.get_ascent","title":"get_ascent  <code>method descriptor</code>","text":"<pre><code>get_ascent() -&gt; int\n</code></pre> <p>get_ascent() -&gt; int get the ascent of the font</p>"},{"location":"api/#pygame.font.Font.get_bold","title":"get_bold  <code>method descriptor</code>","text":"<pre><code>get_bold() -&gt; bool\n</code></pre> <p>get_bold() -&gt; bool check if text will be rendered bold</p>"},{"location":"api/#pygame.font.Font.get_descent","title":"get_descent  <code>method descriptor</code>","text":"<pre><code>get_descent() -&gt; int\n</code></pre> <p>get_descent() -&gt; int get the descent of the font</p>"},{"location":"api/#pygame.font.Font.get_height","title":"get_height  <code>method descriptor</code>","text":"<pre><code>get_height() -&gt; int\n</code></pre> <p>get_height() -&gt; int get the height of the font</p>"},{"location":"api/#pygame.font.Font.get_italic","title":"get_italic  <code>method descriptor</code>","text":"<pre><code>get_italic() -&gt; bool\n</code></pre> <p>get_italic() -&gt; bool check if the text will be rendered italic</p>"},{"location":"api/#pygame.font.Font.get_linesize","title":"get_linesize  <code>method descriptor</code>","text":"<pre><code>get_linesize() -&gt; int\n</code></pre> <p>get_linesize() -&gt; int get the line space of the font text</p>"},{"location":"api/#pygame.font.Font.get_point_size","title":"get_point_size  <code>method descriptor</code>","text":"<pre><code>get_point_size() -&gt; int\n</code></pre> <p>get_point_size() -&gt; int get the point size of the font</p>"},{"location":"api/#pygame.font.Font.get_strikethrough","title":"get_strikethrough  <code>method descriptor</code>","text":"<pre><code>get_strikethrough() -&gt; bool\n</code></pre> <p>get_strikethrough() -&gt; bool check if text will be rendered with a strikethrough</p>"},{"location":"api/#pygame.font.Font.get_underline","title":"get_underline  <code>method descriptor</code>","text":"<pre><code>get_underline() -&gt; bool\n</code></pre> <p>get_underline() -&gt; bool check if text will be rendered with an underline</p>"},{"location":"api/#pygame.font.Font.metrics","title":"metrics  <code>method descriptor</code>","text":"<pre><code>metrics(object) -&gt; list[tuple[int, int, int, int, int]]\n</code></pre> <p>metrics(text, /) -&gt; list gets the metrics for each character in the passed string</p>"},{"location":"api/#pygame.font.Font.render","title":"render  <code>method descriptor</code>","text":"<pre><code>render() -&gt; Surface\n</code></pre> <p>render(text, antialias, color, bgcolor=None, wraplength=0) -&gt; Surface draw text on a new Surface</p>"},{"location":"api/#pygame.font.Font.set_bold","title":"set_bold  <code>method descriptor</code>","text":"<pre><code>set_bold(object) -&gt; None\n</code></pre> <p>set_bold(bool, /) -&gt; None enable fake rendering of bold text</p>"},{"location":"api/#pygame.font.Font.set_direction","title":"set_direction  <code>method descriptor</code>","text":"<pre><code>set_direction() -&gt; None\n</code></pre> <p>set_direction(direction) -&gt; None set the script direction for text shaping</p>"},{"location":"api/#pygame.font.Font.set_italic","title":"set_italic  <code>method descriptor</code>","text":"<pre><code>set_italic(object) -&gt; None\n</code></pre> <p>set_italic(bool, /) -&gt; None enable fake rendering of italic text</p>"},{"location":"api/#pygame.font.Font.set_linesize","title":"set_linesize  <code>method descriptor</code>","text":"<pre><code>set_linesize(object) -&gt; None\n</code></pre> <p>set_linesize(linesize) -&gt; None set the line space of the font text</p>"},{"location":"api/#pygame.font.Font.set_point_size","title":"set_point_size  <code>method descriptor</code>","text":"<pre><code>set_point_size(object) -&gt; None\n</code></pre> <p>set_point_size(size, /) -&gt; None set the point size of the font</p>"},{"location":"api/#pygame.font.Font.set_script","title":"set_script  <code>method descriptor</code>","text":"<pre><code>set_script(object) -&gt; None\n</code></pre> <p>set_script(str, /) -&gt; None set the script code for text shaping</p>"},{"location":"api/#pygame.font.Font.set_strikethrough","title":"set_strikethrough  <code>method descriptor</code>","text":"<pre><code>set_strikethrough(object) -&gt; None\n</code></pre> <p>set_strikethrough(bool, /) -&gt; None control if text is rendered with a strikethrough</p>"},{"location":"api/#pygame.font.Font.set_underline","title":"set_underline  <code>method descriptor</code>","text":"<pre><code>set_underline(object) -&gt; None\n</code></pre> <p>set_underline(bool, /) -&gt; None control if text is rendered with an underline</p>"},{"location":"api/#pygame.font.Font.size","title":"size  <code>method descriptor</code>","text":"<pre><code>size(object) -&gt; tuple[int, int]\n</code></pre> <p>size(text, /) -&gt; (width, height) determine the amount of space needed to render text</p>"},{"location":"api/#ajishio.net.GameNetClient","title":"ajishio.net.GameNetClient","text":"<p>Thin, protocol-agnostic wrapper around <code>Transport</code>.</p> <p>Sends and receives raw strings/bytes only.  All packet encoding and decoding is the responsibility of the caller.</p>"},{"location":"api/#ajishio.net.GameNetClient.send","title":"send","text":"<pre><code>send(data: str | bytes) -&gt; None\n</code></pre> <p>Send a raw string or bytes to the server.</p>"},{"location":"api/#ajishio.net.GameNetClient.recv","title":"recv","text":"<pre><code>recv() -&gt; bytes | None\n</code></pre> <p>Return the next queued message as raw bytes, or None if inbox is empty.</p>"},{"location":"api/#ajishio.net.GameNetClient.run","title":"run  <code>async</code>","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Yield to the event loop each frame. Use as a background task.</p>"},{"location":"api/#ajishio.c_aqua","title":"ajishio.c_aqua  <code>module-attribute</code>","text":"<pre><code>c_aqua = Color(0, 255, 255)\n</code></pre>"},{"location":"api/#ajishio.c_black","title":"ajishio.c_black  <code>module-attribute</code>","text":"<pre><code>c_black = Color(0, 0, 0)\n</code></pre>"},{"location":"api/#ajishio.c_blue","title":"ajishio.c_blue  <code>module-attribute</code>","text":"<pre><code>c_blue = Color(0, 0, 255)\n</code></pre>"},{"location":"api/#ajishio.c_dkgray","title":"ajishio.c_dkgray  <code>module-attribute</code>","text":"<pre><code>c_dkgray = Color(64, 64, 64)\n</code></pre>"},{"location":"api/#ajishio.c_fuchsia","title":"ajishio.c_fuchsia  <code>module-attribute</code>","text":"<pre><code>c_fuchsia = Color(255, 0, 255)\n</code></pre>"},{"location":"api/#ajishio.c_gray","title":"ajishio.c_gray  <code>module-attribute</code>","text":"<pre><code>c_gray = Color(128, 128, 128)\n</code></pre>"},{"location":"api/#ajishio.c_green","title":"ajishio.c_green  <code>module-attribute</code>","text":"<pre><code>c_green = Color(0, 128, 0)\n</code></pre>"},{"location":"api/#ajishio.c_lime","title":"ajishio.c_lime  <code>module-attribute</code>","text":"<pre><code>c_lime = Color(0, 255, 0)\n</code></pre>"},{"location":"api/#ajishio.c_ltgray","title":"ajishio.c_ltgray  <code>module-attribute</code>","text":"<pre><code>c_ltgray = Color(192, 192, 192)\n</code></pre>"},{"location":"api/#ajishio.c_maroon","title":"ajishio.c_maroon  <code>module-attribute</code>","text":"<pre><code>c_maroon = Color(128, 0, 0)\n</code></pre>"},{"location":"api/#ajishio.c_navy","title":"ajishio.c_navy  <code>module-attribute</code>","text":"<pre><code>c_navy = Color(0, 0, 128)\n</code></pre>"},{"location":"api/#ajishio.c_olive","title":"ajishio.c_olive  <code>module-attribute</code>","text":"<pre><code>c_olive = Color(128, 128, 0)\n</code></pre>"},{"location":"api/#ajishio.c_orange","title":"ajishio.c_orange  <code>module-attribute</code>","text":"<pre><code>c_orange = Color(255, 160, 64)\n</code></pre>"},{"location":"api/#ajishio.c_purple","title":"ajishio.c_purple  <code>module-attribute</code>","text":"<pre><code>c_purple = Color(128, 0, 128)\n</code></pre>"},{"location":"api/#ajishio.c_red","title":"ajishio.c_red  <code>module-attribute</code>","text":"<pre><code>c_red = Color(255, 0, 0)\n</code></pre>"},{"location":"api/#ajishio.c_silver","title":"ajishio.c_silver  <code>module-attribute</code>","text":"<pre><code>c_silver = Color(192, 192, 192)\n</code></pre>"},{"location":"api/#ajishio.c_teal","title":"ajishio.c_teal  <code>module-attribute</code>","text":"<pre><code>c_teal = Color(0, 128, 128)\n</code></pre>"},{"location":"api/#ajishio.c_white","title":"ajishio.c_white  <code>module-attribute</code>","text":"<pre><code>c_white = Color(255, 255, 255)\n</code></pre>"},{"location":"api/#ajishio.c_yellow","title":"ajishio.c_yellow  <code>module-attribute</code>","text":"<pre><code>c_yellow = Color(255, 255, 0)\n</code></pre>"},{"location":"api/#ajishio.delta_time","title":"ajishio.delta_time  <code>module-attribute</code>","text":"<pre><code>delta_time: float\n</code></pre>"},{"location":"api/#ajishio.mb_left","title":"ajishio.mb_left  <code>module-attribute</code>","text":"<pre><code>mb_left: int = 1\n</code></pre>"},{"location":"api/#ajishio.mb_middle","title":"ajishio.mb_middle  <code>module-attribute</code>","text":"<pre><code>mb_middle: int = 2\n</code></pre>"},{"location":"api/#ajishio.mb_right","title":"ajishio.mb_right  <code>module-attribute</code>","text":"<pre><code>mb_right: int = 3\n</code></pre>"},{"location":"api/#ajishio.room","title":"ajishio.room  <code>module-attribute</code>","text":"<pre><code>room: int\n</code></pre>"},{"location":"api/#ajishio.room_background_color","title":"ajishio.room_background_color  <code>module-attribute</code>","text":"<pre><code>room_background_color: Color\n</code></pre>"},{"location":"api/#ajishio.room_height","title":"ajishio.room_height  <code>module-attribute</code>","text":"<pre><code>room_height: float\n</code></pre>"},{"location":"api/#ajishio.room_speed","title":"ajishio.room_speed  <code>module-attribute</code>","text":"<pre><code>room_speed: float\n</code></pre>"},{"location":"api/#ajishio.room_width","title":"ajishio.room_width  <code>module-attribute</code>","text":"<pre><code>room_width: float\n</code></pre>"},{"location":"api/#ajishio.view_current","title":"ajishio.view_current  <code>module-attribute</code>","text":"<pre><code>view_current: int\n</code></pre>"},{"location":"api/#ajishio.view_hport","title":"ajishio.view_hport  <code>module-attribute</code>","text":"<pre><code>view_hport = view_hport\n</code></pre>"},{"location":"api/#ajishio.view_wport","title":"ajishio.view_wport  <code>module-attribute</code>","text":"<pre><code>view_wport = view_wport\n</code></pre>"},{"location":"api/#ajishio.view_xport","title":"ajishio.view_xport  <code>module-attribute</code>","text":"<pre><code>view_xport = view_xport\n</code></pre>"},{"location":"api/#ajishio.view_yport","title":"ajishio.view_yport  <code>module-attribute</code>","text":"<pre><code>view_yport = view_yport\n</code></pre>"},{"location":"api/#ajishio.vk_backspace","title":"ajishio.vk_backspace  <code>module-attribute</code>","text":"<pre><code>vk_backspace: int = K_BACKSPACE\n</code></pre>"},{"location":"api/#ajishio.vk_down","title":"ajishio.vk_down  <code>module-attribute</code>","text":"<pre><code>vk_down: int = K_DOWN\n</code></pre>"},{"location":"api/#ajishio.vk_enter","title":"ajishio.vk_enter  <code>module-attribute</code>","text":"<pre><code>vk_enter: int = K_RETURN\n</code></pre>"},{"location":"api/#ajishio.vk_escape","title":"ajishio.vk_escape  <code>module-attribute</code>","text":"<pre><code>vk_escape: int = K_ESCAPE\n</code></pre>"},{"location":"api/#ajishio.vk_left","title":"ajishio.vk_left  <code>module-attribute</code>","text":"<pre><code>vk_left: int = K_LEFT\n</code></pre>"},{"location":"api/#ajishio.vk_right","title":"ajishio.vk_right  <code>module-attribute</code>","text":"<pre><code>vk_right: int = K_RIGHT\n</code></pre>"},{"location":"api/#ajishio.vk_space","title":"ajishio.vk_space  <code>module-attribute</code>","text":"<pre><code>vk_space: int = K_SPACE\n</code></pre>"},{"location":"api/#ajishio.vk_up","title":"ajishio.vk_up  <code>module-attribute</code>","text":"<pre><code>vk_up: int = K_UP\n</code></pre>"},{"location":"api/#ajishio.window_height","title":"ajishio.window_height  <code>module-attribute</code>","text":"<pre><code>window_height: int\n</code></pre>"},{"location":"api/#ajishio.window_width","title":"ajishio.window_width  <code>module-attribute</code>","text":"<pre><code>window_width: int\n</code></pre>"},{"location":"contributing/","title":"Contributing &amp; Local Docs","text":""},{"location":"contributing/#api-reference-generation","title":"API reference generation","text":"<p>The API reference page at docs/api.md is generated automatically from the public <code>ajishio</code> facade exports (<code>ajishio.__all__</code>) by scripts/gen_api_ref.py.</p> <p>Do not edit docs/api.md by hand.</p> <p>Regenerate locally:</p> <pre><code>uv run python scripts/gen_api_ref.py\n</code></pre> <p>Verify it is up to date (used by CI):</p> <pre><code>uv run python scripts/gen_api_ref.py --check\n</code></pre>"},{"location":"contributing/#running-docs-locally","title":"Running docs locally","text":"<p>API docs are generated from the <code>ajishio</code> source using MkDocs Material. Because <code>gen_api_ref.py</code> imports <code>ajishio</code> at build time (which in turn imports pygame-ce), the <code>AJISHIO_DOCS=1</code> environment variable must be set so the engine skips pygame initialisation:</p> <pre><code>AJISHIO_DOCS=1 uv run mkdocs serve\n</code></pre> <p>Then open http://127.0.0.1:8000 in your browser. To build a static site instead:</p> <pre><code>AJISHIO_DOCS=1 uv run mkdocs build --clean\n</code></pre> <p>The generated site is written to <code>site/</code>.</p>"},{"location":"contributing/#why-ci-does-not-open-a-pygame-window","title":"Why CI does not open a pygame window","text":"<p>The API generator imports <code>ajishio</code> for introspection, but it forces a headless SDL backend (<code>SDL_VIDEODRIVER=dummy</code>) during that import. This allows pygame initialization to succeed in CI without creating a real window.</p> <p>The docs workflow also runs:</p> <pre><code>uv run python scripts/gen_api_ref.py --check\n</code></pre> <p>before MkDocs build, so documentation deploy fails if docs/api.md is stale.</p>"},{"location":"demo-projects/","title":"Web Demos","text":"<p>Play Ajishio demo projects instantly in your browser! These are built automatically from the latest <code>main</code> branch and hosted on GitHub Pages.</p> Demo Play Link Description Platformer Play Platformer with LDtk and Aseprite integration Pong Play Classic Pong Snake Play Classic Snake Sokoban Play Push boxes to targets Visual Novel Play Lightweight VN demo Hello World Play Minimal Ajishio setup Spinning Cube Play 3D cube demo using Ajishio rendering Minesweeper Play Minesweeper clone Tetris Play Tetris clone Roguelike Play Roguelike demo Witch Game Play Action platformer Space Invaders Play Space Invaders clone Multiplayer client Play Experimental multiplayer demo client <p>Some demos are experimental or may have limited browser support. </p> <p>These builds use pygbag and a custom Ajishio loading screen. See the repository for source code and instructions.</p>"},{"location":"profiling/","title":"Profiling","text":"<p>Ajishio ships a <code>@aj.profile</code> decorator. Apply it to your <code>main()</code> function and pass <code>--profile</code> on the command line to collect a <code>cProfile</code> run and save a <code>.prof</code> file to the current working directory (named after the project folder, e.g. <code>platformer.prof</code>):</p> <pre><code>@aj.profile\ndef main() -&gt; None:\n    aj.game_start()\n\nmain()\n</code></pre> <pre><code>uv run -m demo_projects.platformer.main --profile\n</code></pre> <p>This prints a summary of the top 30 hotspots sorted by cumulative time. Without <code>--profile</code> the decorator is a no-op and adds zero overhead.</p>"},{"location":"profiling/#visualising-results","title":"Visualising Results","text":"<p>snakeviz is included in the dev dependency group. Open a saved <code>.prof</code> file with:</p> <pre><code>uv run snakeviz platformer.prof\n</code></pre> <p>This starts a local web server and opens an interactive flame-graph in your browser. Press <code>Ctrl-C</code> to stop it.</p> <p>Profiling is not supported on the web export.</p>"},{"location":"rooms/","title":"Loading Rooms","text":"<p>Currently, Ajishio only supports loading rooms in LDtk format. First download the LDtk editor from the website, and create your room (there are some good tutorials out there on how to do this). Once you have your room, make sure to enable the Super simple export option in the Project Settings tab. This will export the room as a set of files in a folder that Ajishio can load directly.</p> <p></p> <p>Tip: Save your LDtk project directly in your project directory so you can re-export without copying files manually.</p> <p>Copy the <code>simplified/</code> folder generated by LDtk into your project directory. Then load it with <code>aj.load_ldtk_levels</code>, passing the path to <code>simplified/</code> as the argument. It returns a list of <code>aj.GameLevel</code> objects to pass to <code>aj.set_rooms</code>.</p> <p>If you have visual-only layers (for example an LDtk auto-layer export that should render but not spawn gameplay objects), pass their names via <code>cosmetic_layers</code>:</p> <pre><code>rooms = aj.load_ldtk_levels(\n    project_dir / \"rooms\" / \"world\" / \"simplified\",\n    cosmetic_layers={\"FloorIntGrid\"},\n)\n</code></pre> <p>In order for the engine to instantiate your tiles and entities, define classes with the same name as they appear in LDtk and register them with <code>aj.register_objects</code> before calling <code>aj.game_start</code>.</p> <p>See the <code>platformer</code> demo project for a full example.</p>"},{"location":"sprites/","title":"Sprites &amp; Collision","text":""},{"location":"sprites/#loading-sprites","title":"Loading Sprites","text":"<p>Ajishio supports loading sprites from Aseprite files. Create your (animated) sprite in Aseprite and export it as both a <code>.png</code> sprite sheet and a <code>.json</code> data file, both into the same directory in your project.</p> <p></p> <p>Load sprites with <code>aj.load_aseprite_sprites</code>, passing the directory path. It returns a <code>dict[str, aj.GameSprite]</code> keyed by the sprite name you saved in Aseprite:</p> <pre><code>class Player(aj.PhysicsObject):\n    def __init__(self, x: float = 0, y: float = 0, **kwargs: Unpack[aj.GameObjectKwargs]) -&gt; None:\n        super().__init__(x, y, **kwargs)\n        self.sprite_index = sprites['player']\n        self.image_speed = 10\n</code></pre> <p>Ajishio automatically animates the sprite at <code>image_speed</code> frames per second. Make sure to call <code>super().step()</code> and <code>super().draw()</code> in any overridden <code>step</code>/<code>draw</code> methods.</p> <p>See the <code>platformer</code> demo for a full example.</p>"},{"location":"sprites/#collision-masks","title":"Collision Masks","text":"<p>Only rectangular collision masks are currently supported. Set the <code>collision_mask</code> attribute in the constructor, typically using <code>sprite_width</code> and <code>sprite_height</code> as a starting point:</p> <pre><code>class Player(aj.GameObject):\n    def __init__(self, x: float = 0, y: float = 0, **kwargs: Unpack[aj.GameObjectKwargs]) -&gt; None:\n        super().__init__(x, y, **kwargs)\n        self.sprite_index = sprites['player']\n        self.collision_mask = aj.CollisionMask(\n            bbtop=3, bbleft=3, bbright=self.sprite_width - 3, bbbottom=self.sprite_height\n        )\n</code></pre> <p>Bounding box coordinates are relative to the top-left corner of the sprite.</p>"},{"location":"vs-code/","title":"VS Code Integration","text":"<p>It is recommended to install the Python extension before editing your game in VS Code. Once it is installed, ensure that the Python interpreter is set to the one in the virtual environment you created earlier. You can do this by opening the command palette (<code>Ctrl+Shift+P</code>) and typing <code>Python: Select Interpreter</code> and choose <code>./.venv/bin/python</code> or <code>./.venv/Scripts/python.exe</code> on Windows.</p> <p></p>"},{"location":"vs-code/#debugging-with-f5","title":"Debugging with F5","text":"<p>You can set up a launch task to debug your game with F5. Add an entry to the <code>.vscode/launch.json</code> file's <code>\"configurations\"</code> array:</p> <pre><code>{\n    \"name\": \"&lt;your project name&gt;\",\n    \"type\": \"debugpy\",\n    \"request\": \"launch\",\n    \"module\": \"&lt;your.dot.separated.project.directory&gt;.main\"\n}\n</code></pre> <p>For example, if your game script is at <code>my_stuff/games/ziltoid_the_destroyer/main.py</code>, your <code>launch.json</code> should look like:</p> <pre><code>{\n    \"version\": \"0.2.0\",\n    \"configurations\": [\n        {\n            \"name\": \"Ziltoid the Destroyer\",\n            \"type\": \"debugpy\",\n            \"request\": \"launch\",\n            \"module\": \"my_stuff.games.ziltoid_the_destroyer.main\"\n        }\n    ]\n}\n</code></pre> <p>Go to the debug tab (<code>Ctrl+Shift+D</code>), select your project from the dropdown, and press F5.</p> <p></p>"},{"location":"vs-code/#static-analysis","title":"Static Analysis","text":"<p>Ajishio is fully typed and works well with basedpyright and ruff. Install the corresponding VS Code extensions to get real-time type checking and linting.</p>"},{"location":"vs-code/#runtime-globals-and-typing","title":"Runtime globals and typing","text":"<p>All public API \u2014 functions, classes, constants, and live engine/view state \u2014 is available directly on the <code>ajishio</code> package via a single <code>import ajishio as aj</code>. Callables, data classes, and constants are module-level names resolved at import time. Live per-frame values such as <code>aj.room_width</code>, <code>aj.delta_time</code>, <code>aj.room_speed</code>, <code>aj.view_current</code>, <code>aj.window_width</code>, and <code>aj.room_background_color</code> are resolved dynamically via <code>__getattr__</code> so they always reflect the current engine state. View port dictionaries (<code>aj.view_xport</code>, <code>aj.view_yport</code>, <code>aj.view_wport</code>, <code>aj.view_hport</code>) are exported as direct references to the live mutable dicts on the <code>View</code> singleton, so reads and writes to them are always live.</p>"},{"location":"web-export/","title":"Web Export","text":"<p>Ajishio projects can be exported to run in the browser via pygbag. No changes to your game code are required.</p>"},{"location":"web-export/#usage","title":"Usage","text":"<p>From the repository root:</p> <pre><code>uv run pygbag_export.py demo_projects/sokoban\n</code></pre> <p>This starts a local test server at http://localhost:8000. Open it in a Chromium-based browser (Chrome, Edge, Brave) for best results.</p> <p>Other useful flags:</p> <pre><code># Build only, don't start the server\nuv run pygbag_export.py demo_projects/sokoban --build\n\n# Build a zip you can upload to itch.io\nuv run pygbag_export.py demo_projects/sokoban --build --archive\n</code></pre> <p>Output goes to <code>&lt;your_project&gt;/_web_build/</code>.</p>"},{"location":"web-export/#what-the-export-script-does","title":"What the export script does","text":"<p>The script takes care of everything so you don't have to think about it:</p> <ol> <li>Copies your project into a temporary build directory (your source files are never modified)</li> <li>Bundles the ajishio library alongside your project</li> <li>Patches the copied <code>main.py</code> for the browser:<ul> <li>Adds <code>import pygame</code> so pygbag downloads the pygame-ce WebAssembly wheel</li> <li>Adds <code>import asyncio</code> if not already present</li> <li>Swaps <code>aj.game_start()</code> \u2192 <code>asyncio.run(aj.game_start_async())</code> so the game loop yields   to the browser every frame</li> </ul> </li> </ol>"},{"location":"web-export/#debugging","title":"Debugging","text":"<p>If you see a blank grey screen, append <code>?-i</code> to the URL (e.g. http://localhost:8000?-i). This opens a Python terminal at the bottom of the page where you can see tracebacks and print output.</p>"},{"location":"web-export/#known-limitations","title":"Known limitations","text":"<ul> <li><code>cProfile</code> doesn't exist in WebAssembly. Ajishio handles this internally, but avoid   importing <code>cProfile</code> or <code>pstats</code> directly in your game code.</li> <li><code>from __future__ import annotations</code> \u2014 add this to your <code>main.py</code> if you use type hints   behind <code>if TYPE_CHECKING</code> guards. pygbag evaluates annotations eagerly.</li> </ul>"},{"location":"net/","title":"Multiplayer Networking Internals","text":""},{"location":"net/#table-of-contents","title":"Table of Contents","text":"<ol> <li>What pygbag Actually Does</li> <li>The Browser Runtime Environment</li> <li>The <code>aio</code> Module</li> <li>The <code>js</code> Module</li> <li>The <code>aio.cross</code> Module</li> <li>The Simulator vs Real Browser</li> <li>Networking Strategies</li> <li>The +20000 Port Mapping</li> <li>Type Checking Across Platforms</li> <li>The Stub Files</li> <li>The Dual-Field Transport Design</li> <li>Platform Abstraction Helpers</li> <li>The <code>GameNetClient</code> API and <code>packets.py</code></li> <li>The <code>asyncio</code> Hijack</li> <li>Data Flow Reference</li> <li>Known Gotchas</li> <li>Extending This Into the Engine</li> </ol>"},{"location":"net/01-what-pygbag-actually-does/","title":"What pygbag Actually Does","text":""},{"location":"net/01-what-pygbag-actually-does/#1-what-pygbag-actually-does","title":"1. What pygbag Actually Does","text":"<p>pygbag is a command-line tool that takes a Python/pygame project and compiles it to run in a browser via WebAssembly (WASM).  Understanding what it does at each stage is the key to understanding every networking decision in this demo.</p>"},{"location":"net/01-what-pygbag-actually-does/#build-phase","title":"Build phase","text":"<p>When you run <code>pygbag .</code> inside the client directory, it:</p> <ol> <li>Packages all Python files and assets into a WASM-loadable archive.</li> <li>Wraps CPython compiled to WASM (via Emscripten) in an HTML/JS shell.</li> <li>Serves the result over a local HTTP dev server (default port 8000).</li> </ol>"},{"location":"net/01-what-pygbag-actually-does/#runtime-phase","title":"Runtime phase","text":"<p>Once the browser loads the page:</p> <ol> <li>The Emscripten-compiled CPython boots inside a Web Worker.</li> <li>Before your <code>main.py</code> runs, pygbag injects a set of \"support modules\"    directly into <code>sys.modules</code> \u2014 these are not files on disk that you can    <code>import</code> normally; they are constructed entirely in memory and registered    by pygbag's bootstrap code.</li> <li>Your code then runs inside a cooperative async event loop that is ticked    by <code>window.requestAnimationFrame()</code> \u2014 meaning the event loop advances once    per display frame (~60 times per second), not continuously.</li> </ol> <p>The critical injected modules are <code>aio</code>, <code>aio.cross</code>, and <code>js</code>.  There is no PyPI package for any of them.  They do not exist on desktop.  This is the entire source of the cross-platform complexity.</p>"},{"location":"net/02-the-browser-runtime-environment/","title":"The Browser Runtime Environment","text":""},{"location":"net/02-the-browser-runtime-environment/#2-the-browser-runtime-environment","title":"2. The Browser Runtime Environment","text":""},{"location":"net/02-the-browser-runtime-environment/#sysplatform","title":"<code>sys.platform</code>","text":"<p>Inside the WASM runtime, <code>sys.platform == \"emscripten\"</code>.  This is the single most reliable way to detect you are running in the browser:</p> <pre><code>_IS_BROWSER = sys.platform == \"emscripten\"\n</code></pre> <p>basedpyright does NOT narrow through this intermediate variable, so it always analyses both branches.  This is intentional and important \u2014 it means the type checker sees all code paths, which is what forces us to write fully typed, platform-agnostic code.</p>"},{"location":"net/02-the-browser-runtime-environment/#the-event-loop","title":"The event loop","text":"<p>The WASM asyncio loop is not driven by <code>asyncio.run()</code> in the normal sense. Instead, pygbag replaces <code>asyncio</code> with its own <code>aio</code> module (see \u00a73) and drives the loop from a JavaScript <code>requestAnimationFrame</code> callback.</p> <p>Consequence: you must <code>await</code> frequently.  Any synchronous code that runs for more than ~16 ms will freeze the browser tab.  Every hot loop must contain an <code>await sleep0()</code> (or equivalent) to yield control back to the browser.</p>"},{"location":"net/02-the-browser-runtime-environment/#sockets","title":"Sockets","text":"<p>WASM Python has a socket module, but the browser sandbox means raw TCP connections are not possible.  Instead, pygbag provides a WebSocket-to-TCP proxy bridge.  From Python, you use <code>socket.socket()</code> as normal; under the hood, each connect/send/recv is tunnelled through a WebSocket connection managed by JavaScript.  This is transparent to Python code but has consequences for port numbers (see \u00a78).</p>"},{"location":"net/03-the-aio-module/","title":"The aio Module","text":""},{"location":"net/03-the-aio-module/#3-the-aio-module","title":"3. The <code>aio</code> Module","text":""},{"location":"net/03-the-aio-module/#what-it-is","title":"What it is","text":"<p><code>aio</code> is pygbag's custom async runtime shim.  Its source lives inside the pygbag package at:</p> <pre><code>pygbag/support/cross/aio/__init__.py\n</code></pre> <p>It is injected at startup via:</p> <pre><code>builtins.aio = sys.modules[__name__]  # inside aio/__init__.py\n</code></pre> <p>This means <code>aio</code> is available as a builtin \u2014 you can <code>import aio</code> from anywhere inside the WASM runtime without it being a \"real\" installed package.</p>"},{"location":"net/03-the-aio-module/#what-it-contains","title":"What it contains","text":"<p><code>aio</code> starts by doing <code>from asyncio import *</code>, which means it re-exports the entire standard asyncio API.  It then overrides or augments specific pieces:</p> Name Type Description <code>aio.exit</code> <code>bool</code> Set to <code>True</code> by pygbag when the app should close.  Poll this in your main loop to know when to stop. <code>aio.started</code> <code>bool</code> <code>True</code> once the event loop has been started. <code>aio.paused</code> <code>bool</code> <code>True</code> when the loop is suspended (e.g., loading screen). <code>aio.sleep(delay)</code> <code>Awaitable[None]</code> Identical to <code>asyncio.sleep</code>.  Yields control for at least <code>delay</code> seconds.  <code>aio.sleep(0)</code> yields for one frame. <code>aio.create_task(coro)</code> <code>Task[T]</code> Identical to <code>asyncio.create_task</code>.  Schedules a coroutine on the running loop. <code>aio.run(coro)</code> <code>None</code> Like <code>asyncio.run</code>, but plugs into the frame-based scheduler instead of blocking. <code>aio.loop</code> <code>AbstractEventLoop</code> The single shared event loop for the WASM runtime. <code>aio.cross</code> module Platform-detection submodule (see \u00a75)."},{"location":"net/03-the-aio-module/#the-from-asyncio-import-trick","title":"The <code>from asyncio import *</code> trick","text":"<p>Because <code>aio</code> re-exports all of <code>asyncio</code>, code that did <code>import asyncio</code> and then uses <code>asyncio.sleep</code>, <code>asyncio.create_task</code> etc. will mostly work even in the browser \u2014 because at module-level, <code>sys.modules[\"asyncio\"] = aio</code>. That line is the last thing <code>aio/__init__.py</code> does:</p> <pre><code>sys.modules[\"asyncio\"] = __import__(__name__)\n</code></pre> <p>So after <code>aio</code> loads, <code>import asyncio</code> gives you <code>aio</code>.  This is why the same <code>asyncio.sleep(0)</code> call works on both desktop and browser \u2014 on browser it's actually <code>aio.sleep(0)</code> in disguise.</p>"},{"location":"net/03-the-aio-module/#why-we-dont-just-use-asyncio-everywhere","title":"Why we don't just use <code>asyncio</code> everywhere","text":"<p>The important divergences are:</p> <ul> <li><code>aio.exit</code> \u2014 no equivalent in real asyncio.</li> <li><code>aio.create_task</code> stores created tasks in an internal list so pygbag can   cancel them on shutdown.  Real <code>asyncio.create_task</code> does not.</li> <li><code>aio.sleep(0)</code> advances the frame-based loop; <code>asyncio.sleep(0)</code> on   desktop advances the I/O-based loop.  The effect is the same for our   purposes, but the implementation path is different.</li> </ul> <p>Using the <code>aio.*</code> names directly (instead of relying on the <code>sys.modules</code> hijack) is more explicit and makes the browser-specific intent clear.</p>"},{"location":"net/04-the-js-module/","title":"The js Module","text":""},{"location":"net/04-the-js-module/#4-the-js-module","title":"4. The <code>js</code> Module","text":""},{"location":"net/04-the-js-module/#what-it-is","title":"What it is","text":"<p><code>js</code> is a proxy to the browser's global JavaScript namespace.  Its source of truth on Emscripten is a single line inside <code>pygbag/support/cross/__EMSCRIPTEN__.py</code>:</p> <pre><code>sys.modules[\"js\"] = window.globalThis\n</code></pre> <p><code>window.globalThis</code> is an Emscripten embed bridge object that reflects the browser's <code>window</code> global.  Every property of <code>window</code> becomes an attribute of the <code>js</code> module.  Every method of <code>window</code> becomes callable from Python.</p>"},{"location":"net/04-the-js-module/#what-this-means-in-practice","title":"What this means in practice","text":"<pre><code>import js\n\n# Read a browser global\norigin = js.location.origin          # \u2192 \"http://localhost:8000\"\n\n# Call a browser API\njs.console.log(\"hello from Python\")\n\n# Evaluate arbitrary JavaScript\nresult = js.eval(\"1 + 1\")            # \u2192 2 (as a Python int proxy)\n\n# Create a JS object\nws = js.eval(f\"new WebSocket({url!r})\")\n</code></pre> <p><code>js.eval(code: str)</code> is the escape hatch for anything that the Emscripten bridge does not expose as a direct attribute.  It evaluates <code>code</code> in the browser's JS engine and returns the result as a Python proxy object.</p>"},{"location":"net/04-the-js-module/#the-jswebsocket-class","title":"The <code>js.WebSocket</code> class","text":"<p>Because <code>js</code> mirrors <code>window</code>, and <code>window.WebSocket</code> is the browser's native WebSocket class, <code>js.WebSocket</code> is the browser's WebSocket constructor. However, creating a WebSocket via <code>js.WebSocket(url)</code> directly from Python does not always work reliably across all pygbag versions.  The safe pattern used in this codebase is:</p> <pre><code>self._js_ws = cast(js.WebSocket, js.eval(f\"new WebSocket({json.dumps(url)})\"))\n</code></pre> <p>Using <code>json.dumps(url)</code> ensures the URL string is correctly quoted for JavaScript, including handling special characters.</p>"},{"location":"net/04-the-js-module/#js-objects-from-pythons-perspective","title":"JS objects from Python's perspective","text":"<p>A value returned from <code>js.eval()</code> is a proxy object that forwards attribute access and method calls into JavaScript.</p> <ul> <li>Attribute access works normally: <code>ws.readyState</code>, <code>ws.binaryType</code></li> <li>Method calls work normally: <code>ws.send(\"hello\")</code>, <code>ws.close()</code></li> <li>Event handler assignment works by direct Python assignment:   <code>ws.onmessage = my_python_function</code></li> </ul> <p>For binary events, the current transport does not rely on <code>.to_py()</code> on <code>ArrayBuffer</code> objects. Instead, it converts via JavaScript explicitly:</p> <pre><code>csv = cast(\n    js.JSFunction, js.eval(\"(x) =&gt; Array.from(new Uint8Array(x)).join(',')\")\n)(payload)\nreturn bytes(int(part) for part in str(csv).split(\",\"))\n</code></pre> <p>This avoids runtime differences where <code>.to_py()</code> may be unavailable or inconsistent in pygbag environments.</p>"},{"location":"net/04-the-js-module/#the-js-module-on-desktop","title":"The <code>js</code> module on desktop","text":"<p><code>js</code> does not exist on desktop.  It is only available inside the WASM runtime. Attempting to <code>import js</code> on desktop raises <code>ModuleNotFoundError</code>.  This is why every real import of <code>js</code> is either:</p> <ul> <li>Inside an <code>if _IS_BROWSER:</code> block, or</li> <li>Inside a <code>try: import js</code> block, or</li> <li>Under <code>if TYPE_CHECKING:</code> only (for type annotations \u2014 never executed).</li> </ul>"},{"location":"net/05-the-aio-cross-module/","title":"The aio.cross Module","text":""},{"location":"net/05-the-aio-cross-module/#5-the-aiocross-module","title":"5. The <code>aio.cross</code> Module","text":""},{"location":"net/05-the-aio-cross-module/#what-it-is","title":"What it is","text":"<p><code>aio.cross</code> is a submodule of <code>aio</code> that holds platform-detection state.  Its source is at <code>pygbag/support/cross/aio/cross.py</code>.</p>"},{"location":"net/05-the-aio-cross-module/#key-attributes","title":"Key attributes","text":"Name Type Default Meaning <code>aio.cross.simulator</code> <code>bool \\| None</code> <code>None</code> <code>True</code> when running in the pygbag desktop simulator (see \u00a76).  <code>None</code> or <code>False</code> when running in a real browser or on desktop. <code>aio.cross.scheduler</code> <code>callable \\| None</code> <code>None</code> The function pygbag uses to reschedule the event loop step.  Internal use only."},{"location":"net/05-the-aio-cross-module/#why-we-care-about-simulator","title":"Why we care about <code>simulator</code>","text":"<p>The simulator and the real browser handle WebSocket/socket connections differently \u2014 particularly the URL format.  In the simulator, the server address is embedded in the URL path in a special format, rather than being a plain <code>host:port</code> pair.  The <code>aio_sock_open</code> function in <code>net.py</code> checks this:</p> <pre><code>if getattr(aio.cross, \"simulator\", False):\n    if \"/\" in host:\n        host, trail = host.strip(\":/\").split(\"/\", 1)\n        port = int(trail.rsplit(\":\", 1)[-1])\n</code></pre> <p>We use <code>getattr(..., \"simulator\", False)</code> as a defensive call because <code>aio</code> is only available in the browser, and <code>aio.cross.simulator</code> could theoretically be <code>None</code> rather than <code>False</code> in some contexts.</p> <p>In this repository's <code>Transport</code>, this check only affects the browser fallback socket path (inside <code>_connect_browser</code>).  If <code>_IS_BROWSER</code> is false, the desktop websockets path is used and <code>aio.cross.simulator</code> is not consulted.</p>"},{"location":"net/06-the-simulator-vs-real-browser/","title":"Simulator vs Real Browser","text":""},{"location":"net/06-the-simulator-vs-real-browser/#6-the-simulator-vs-real-browser","title":"6. The Simulator vs Real Browser","text":"<p>pygbag has two runtime modes:</p>"},{"location":"net/06-the-simulator-vs-real-browser/#real-browser-emscripten-sysplatform-emscripten","title":"Real browser (Emscripten, <code>sys.platform == \"emscripten\"</code>)","text":"<ul> <li>CPython compiled to WASM runs inside a browser tab.</li> <li><code>js</code> module is the real <code>window.globalThis</code>.</li> <li><code>js.WebSocket</code> is the real browser WebSocket API.</li> <li>Network connections go through the browser's network stack.</li> <li><code>aio.cross.simulator</code> is <code>None</code> / falsy.</li> </ul>"},{"location":"net/06-the-simulator-vs-real-browser/#desktop-simulator-pygbagaio-non-emscripten","title":"Desktop simulator (<code>pygbag.aio</code>, non-emscripten)","text":"<ul> <li>pygbag provides a Python-side simulation of the WASM environment that runs   on your normal desktop Python.</li> <li>Useful for iteration without having to compile to WASM.</li> <li><code>aio</code> is still injected (via <code>pygbag/aio.py</code> which imports the support   module and runs a desktop event loop).</li> <li><code>js</code> is injected differently \u2014 the desktop simulator uses a stub.</li> <li>Socket connections use a different URL encoding format.</li> <li><code>aio.cross.simulator</code> is <code>True</code>.</li> </ul>"},{"location":"net/06-the-simulator-vs-real-browser/#what-this-means-for-you","title":"What this means for you","text":"<p>When testing networking locally, you may find yourself in the simulator.</p> <p>In this repository's current <code>Transport</code> implementation, simulator-specific <code>aio.cross.simulator</code> logic is only used in the browser fallback path.  The desktop simulator path (<code>_IS_BROWSER == False</code>) goes through desktop <code>websockets.connect(...)</code>.</p> <p>Always test in a real browser before assuming simulator behaviour matches production.</p>"},{"location":"net/07-networking-strategies/","title":"Networking Strategies","text":""},{"location":"net/07-networking-strategies/#7-networking-strategies","title":"7. Networking Strategies","text":"<p>The transport layer in <code>net.py</code> supports three distinct networking paths, selected at runtime based on what is available:</p> <p>The transport in <code>ajishio/src/ajishio/net.py</code> has three runtime paths.</p>"},{"location":"net/07-networking-strategies/#path-a-browser-js-websocket-preferred","title":"Path A: Browser JS WebSocket (preferred)","text":"<p>Used when <code>_IS_BROWSER</code> is true and <code>js.WebSocket</code> exists.</p> <pre><code>connect() \u2192 _connect_browser()\n    \u2192 js.eval(\"new WebSocket(...)\")\n    \u2192 register onopen/onmessage/onerror/onclose\n    \u2192 inbox receives bytes\n</code></pre> <p>Current behavior: - Messages are buffered as <code>bytes</code> in <code>self.inbox</code>. - Text frames are encoded with UTF-8 before enqueue. - Binary frames are converted from <code>ArrayBuffer</code> via JS <code>Uint8Array</code> helper,   then enqueued as <code>bytes</code>. - Callback references are stored on <code>self</code> (<code>_on_open</code>, <code>_on_message</code>, etc.) to   avoid callback proxy lifetime issues.</p>"},{"location":"net/07-networking-strategies/#path-b-browser-socket-fallback","title":"Path B: Browser socket fallback","text":"<p>Used when browser mode is active but JS WebSocket creation is unavailable or fails.</p> <pre><code>connect() \u2192 _connect_browser()\n    \u2192 socket.socket()\n    \u2192 aio_sock_open(...)\n    \u2192 run_task(_reader_browser())\n</code></pre> <p>Current behavior: - Uses non-blocking socket polling with <code>select.select(..., timeout=0)</code>. - Received chunks are appended directly as <code>bytes</code>. - <code>recv()</code> returns from the shared <code>inbox</code>, so fallback and non-fallback   paths consume data through the same queue. - For non-simulator URLs, the fallback applies <code>port += 20000</code>.</p> <p>Important caveat: - <code>_parse_url</code> requires <code>host:port</code> style endpoints. A URL without explicit   port (for example <code>wss://example.com/path</code>) cannot use this fallback path.</p>"},{"location":"net/07-networking-strategies/#path-c-desktop-websockets","title":"Path C: Desktop websockets","text":"<p>Used when <code>_IS_BROWSER</code> is false.</p> <pre><code>connect() \u2192 _connect_desktop()\n    \u2192 await websockets.connect(url)\n    \u2192 run_task(_reader_desktop())\n</code></pre> <p>Current behavior: - Reader appends either binary payloads or UTF-8 encoded text payloads to   <code>self.inbox</code> as <code>bytes</code>. - <code>send()</code> schedules <code>self._desktop_ws.send(...)</code> via <code>run_task</code>.</p>"},{"location":"net/08-the-20000-port-mapping/","title":"The +20000 Port Mapping","text":""},{"location":"net/08-the-20000-port-mapping/#8-the-20000-port-mapping","title":"8. The +20000 Port Mapping","text":"<p>This mapping applies only to the socket fallback path in browser mode.</p>"},{"location":"net/08-the-20000-port-mapping/#the-problem","title":"The problem","text":"<p>Browsers cannot make raw TCP connections \u2014 only WebSocket connections. When Python code calls <code>socket.connect((\"localhost\", 8765))</code>, the WASM runtime cannot literally open a TCP socket.  Instead, it sends a WebSocket connection request to the pygbag development server.</p>"},{"location":"net/08-the-20000-port-mapping/#the-solution","title":"The solution","text":"<p>The pygbag dev server (<code>pygbag .</code>) exposes a WebSocket-to-TCP bridge used by browser socket emulation.</p> <p>In the current transport, for non-simulator fallback URLs, the mapped port is:</p> <p><code>mapped_port = original_port + 20000</code></p> <p><code>ajishio/src/ajishio/net.py</code> applies this automatically:</p> <pre><code>if not self.url.startswith(\"://\"):\n    port += 20000\n</code></pre> <p>The <code>\"://\"</code> format is used by simulator-specific addressing; in that case this mapping is skipped.</p>"},{"location":"net/08-the-20000-port-mapping/#practical-implications","title":"Practical implications","text":"<ul> <li>This mapping is irrelevant for desktop mode.</li> <li>This mapping is irrelevant when browser JS WebSocket path is used.</li> <li>It only matters in browser fallback socket mode.</li> </ul> <p>For this demo, the primary path is JS WebSocket. The fallback path exists for compatibility but has stricter URL requirements and different runtime behavior.</p>"},{"location":"net/09-type-checking-across-platforms/","title":"Type Checking Across Platforms","text":""},{"location":"net/09-type-checking-across-platforms/#9-type-checking-across-platforms","title":"9. Type Checking Across Platforms","text":""},{"location":"net/09-type-checking-across-platforms/#the-challenge","title":"The challenge","text":"<p>The type checker (basedpyright) analyses all code paths simultaneously.  It does not narrow based on <code>sys.platform == \"emscripten\"</code> when that check is stored in an intermediate variable like <code>_IS_BROWSER</code>.  This means it sees code that uses <code>js.WebSocket</code> AND code that uses <code>websockets.ClientConnection</code> as potentially running in the same context.</p>"},{"location":"net/09-type-checking-across-platforms/#type_checking-guard","title":"<code>TYPE_CHECKING</code> guard","text":"<p>The solution is the standard Python pattern:</p> <pre><code>from typing import TYPE_CHECKING\n\nif TYPE_CHECKING:\n    import js                                          # never runs at runtime\n    from websockets.asyncio.client import ClientConnection  # never runs at runtime\n</code></pre> <p><code>TYPE_CHECKING</code> is always <code>False</code> at runtime and always <code>True</code> for the type checker.  Code inside these blocks exists only for the benefit of the type checker and never executes.  This allows us to write annotations that reference <code>js.WebSocket</code> and <code>ClientConnection</code> without causing <code>ImportError</code> on platforms where those modules don't exist.</p>"},{"location":"net/09-type-checking-across-platforms/#string-annotations-for-inner-function-forward-references","title":"String annotations for inner-function forward references","text":"<p>Python 3.14 implements PEP 649 (lazy evaluation of annotations).  Function parameter annotations are not evaluated when the function is defined \u2014 they are only evaluated if something accesses <code>__annotations__</code>.  This means you can safely write:</p> <pre><code>def on_message(event: \"js.MessageEvent\") -&gt; None:\n    ...\n</code></pre> <p>inside a browser-only code block, even though <code>js</code> is not imported at runtime on desktop.  The string <code>\"js.MessageEvent\"</code> is stored as a string, never evaluated.  basedpyright resolves it using the <code>TYPE_CHECKING</code> import.</p> <p>The quoted form is preferred over the bare form for browser-only types defined in nested functions, because it makes the forward-reference intent explicit.</p>"},{"location":"net/09-type-checking-across-platforms/#coroutineany-any-none","title":"<code>Coroutine[Any, Any, None]</code>","text":"<p>Real Python coroutines (from <code>async def f() -&gt; None: ...</code>) have the type <code>Coroutine[Any, Any, None]</code>.  The <code>Any</code> values represent the internal yield and send types, which are implementation details of the coroutine machinery. This is the correct and accurate type annotation for \"a coroutine that returns None\", even though <code>reportExplicitAny</code> flags it.  The suppression comment:</p> <pre><code>def run_task(coro: Coroutine[Any, Any, None]) -&gt; Task[None]:  # pyright: ignore[reportExplicitAny]\n</code></pre> <p>is intentional and correct \u2014 not a hack.</p>"},{"location":"net/09-type-checking-across-platforms/#cast-for-js-proxy-objects","title":"<code>cast()</code> for JS proxy objects","text":"<p><code>js.eval(code)</code> returns <code>JSObject</code> (which is just <code>object</code> in the stubs \u2014 the exact runtime type is unknowable statically).  When we know the returned object is a <code>WebSocket</code>, we use <code>cast</code> to tell the type checker:</p> <pre><code>self._js_ws = cast(js.WebSocket, js.eval(f\"new WebSocket({json.dumps(url)})\"))\n</code></pre> <p><code>cast</code> has no runtime effect \u2014 it returns its second argument unchanged.  This is safe because we are inside <code>if hasattr(js, \"WebSocket\"):</code> which guarantees <code>js.WebSocket</code> exists, and <code>new WebSocket(url)</code> in JS is guaranteed to return a <code>WebSocket</code> instance.</p>"},{"location":"net/10-the-stub-files/","title":"The Stub Files","text":""},{"location":"net/10-the-stub-files/#10-the-stub-files","title":"10. The Stub Files","text":"<p>Because <code>aio</code> and <code>js</code> are injected at runtime and have no installable Python package, basedpyright cannot discover them from site-packages. This repository provides dedicated stubs under <code>ajishio/typings</code>.</p>"},{"location":"net/10-the-stub-files/#how-basedpyright-finds-them","title":"How basedpyright finds them","text":"<p>The workspace config sets:</p> <pre><code>[tool.basedpyright]\nstubPath = \"ajishio/typings\"\n</code></pre> <p>So these files are used globally: - <code>ajishio/typings/js.pyi</code> - <code>ajishio/typings/aio/__init__.pyi</code> - <code>ajishio/typings/aio/cross.pyi</code></p>"},{"location":"net/10-the-stub-files/#ajishiotypingsjspyi","title":"<code>ajishio/typings/js.pyi</code>","text":"<p>Documents the subset of the browser <code>window</code> global that this codebase uses:</p> <ul> <li><code>js.WebSocket</code> \u2014 browser WebSocket class with event handlers and <code>send/close</code>.</li> <li><code>js.Event</code> \u2014 the base event type passed to <code>onopen</code>, <code>onerror</code>, <code>onclose</code>.   The <code>type: str</code> attribute gives the event type name.</li> <li><code>js.MessageEvent</code> \u2014 <code>data</code> is <code>str | ArrayBuffer</code>.</li> <li><code>js.eval(code: str) -&gt; JSObject | JSFunction</code>.</li> </ul> <p>The stub intentionally treats <code>ArrayBuffer</code> and <code>Uint8Array</code> as opaque. The transport converts binary payloads via JS helpers rather than assuming <code>.to_py()</code> support.</p>"},{"location":"net/10-the-stub-files/#ajishiotypingsaio__init__pyi","title":"<code>ajishio/typings/aio/__init__.pyi</code>","text":"<p>Documents the subset used by the transport:</p> <ul> <li><code>aio.exit: bool</code> \u2014 the shutdown flag.  Poll in your main loop.</li> <li><code>aio.sleep(delay: float) -&gt; Awaitable[None]</code> \u2014 identical to <code>asyncio.sleep</code>.</li> <li><code>aio.create_task(coro, ...) -&gt; Task[T]</code> \u2014 identical to <code>asyncio.create_task</code>.</li> <li><code>aio.cross</code> \u2014 re-exported from the <code>cross</code> submodule via relative import.</li> </ul> <p>The relative import keeps resolution scoped to the local stub package.</p>"},{"location":"net/10-the-stub-files/#ajishiotypingsaiocrosspyi","title":"<code>ajishio/typings/aio/cross.pyi</code>","text":"<p>Minimal stub for the platform-detection submodule:</p> <pre><code>simulator: bool\n</code></pre> <p>Runtime code still accesses it defensively via <code>getattr(..., \"simulator\", False)</code>.</p>"},{"location":"net/10-the-stub-files/#maintaining-the-stubs","title":"Maintaining the stubs","text":"<p>When using new <code>js.*</code> or <code>aio.*</code> members, update these stubs and verify against runtime behavior in both desktop and browser paths.</p>"},{"location":"net/11-the-dual-field-transport-design/","title":"The Dual-Field Transport Design","text":""},{"location":"net/11-the-dual-field-transport-design/#11-the-dual-field-transport-design","title":"11. The Dual-Field Transport Design","text":""},{"location":"net/11-the-dual-field-transport-design/#why-one-selfws-field-does-not-work","title":"Why one <code>self.ws</code> field does not work","text":"<p>The original code had:</p> <pre><code>self.ws: \"js.WebSocket | None\" = None\n</code></pre> <p>This type annotation was applied to both the browser JS WebSocket object and the desktop <code>websockets.ClientConnection</code>.  This caused a cascade of type errors because the two types have incompatible interfaces:</p> Operation <code>js.WebSocket</code> <code>websockets.ClientConnection</code> Sending data <code>ws.send(data)</code> \u2014 sync, returns <code>None</code> <code>await ws.send(data)</code> \u2014 async coroutine Closing <code>ws.close()</code> \u2014 sync <code>await ws.close()</code> \u2014 async coroutine Receiving via <code>onmessage</code> callback <code>async for msg in ws:</code> <p>basedpyright, correctly analysing both code paths simultaneously, saw that <code>self.ws.send(data)</code> returned <code>None</code> (from the <code>js.WebSocket</code> stub) and therefore <code>run_task(self.ws.send(data))</code> was passing <code>None</code> to a function expecting a coroutine \u2014 a genuine error.</p>"},{"location":"net/11-the-dual-field-transport-design/#the-fix-two-fields-each-with-the-right-type","title":"The fix: two fields, each with the right type","text":"<pre><code># In Transport.__init__:\nself._js_ws: \"js.WebSocket | None\" = None      # browser path only\nself._desktop_ws: \"ClientConnection | None\" = None  # desktop path only\n</code></pre> <p>Each field is only ever written and read in its own platform's code path. The type checker sees exactly the right type in each context:</p> <ul> <li><code>self._js_ws.send(data)</code> \u2192 <code>None</code> \u2192 no <code>run_task</code> needed \u2192 correct</li> <li><code>self._desktop_ws.send(data)</code> \u2192 <code>Coroutine[Any, Any, None]</code> \u2192 needs <code>run_task</code> \u2192 correct</li> <li><code>async for msg in self._desktop_ws:</code> \u2192 <code>ClientConnection</code> is async-iterable \u2192 correct</li> </ul>"},{"location":"net/11-the-dual-field-transport-design/#the-cast-at-assignment","title":"The <code>cast</code> at assignment","text":"<pre><code>self._js_ws = cast(js.WebSocket, js.eval(f\"new WebSocket({json.dumps(self.url)})\"))\n</code></pre> <p><code>js.eval()</code> is typed as returning <code>JSObject</code> (i.e., <code>object</code>).  <code>cast</code> tells the type checker \"trust me, this is a <code>js.WebSocket</code>\".  This is safe because the surrounding <code>if hasattr(js, \"WebSocket\"):</code> guard confirms the class exists and <code>new WebSocket(url)</code> always returns a <code>WebSocket</code> instance in JS.</p>"},{"location":"net/11-the-dual-field-transport-design/#keeping-js-callbacks-alive-the-gc-trap","title":"Keeping JS callbacks alive: the GC trap","text":"<p>This was discovered by observing that <code>on_message</code> silently never fired in the real browser, even though DevTools confirmed the WebSocket was receiving frames.</p> <p>When you write:</p> <pre><code>def on_message(event): ...\nself._js_ws.onmessage = on_message\n</code></pre> <p>the assignment goes through the Emscripten bridge.  JavaScript does not receive the Python function itself \u2014 it receives a proxy wrapper object.  The proxy holds a weak handle back into the Python heap.  If Python's garbage collector later reclaims the original callable (because nothing else references it), the proxy becomes a dangling pointer.  JavaScript still believes the handler is registered, but calling it either does nothing or crashes silently.</p> <p>The fix is to keep a strong Python reference on the <code>Transport</code> instance for as long as the socket exists:</p> <pre><code># In Transport.__init__:\nself._on_open:    \"Callable[[js.Event], None] | None\"    = None\nself._on_message: \"Callable[[js.MessageEvent], None] | None\" = None\nself._on_error:   \"Callable[[js.Event], None] | None\"    = None\nself._on_close:   \"Callable[[js.Event], None] | None\"    = None\n</code></pre> <p>Then in <code>_connect_browser</code>, after defining the closures, store them before assigning to the JS object:</p> <pre><code>self._on_open    = on_open\nself._on_message = on_message\nself._on_error   = on_error\nself._on_close   = on_close\n\nself._js_ws.onopen    = self._on_open\nself._js_ws.onmessage = self._on_message\nself._js_ws.onerror   = self._on_error\nself._js_ws.onclose   = self._on_close\n</code></pre> <p>Now the <code>Transport</code> object owns a live reference to every callback.  As long as <code>Transport</code> is alive, the callbacks cannot be garbage collected, and the JavaScript proxy remains valid.</p> <p>Symptom checklist \u2014 if you see this bug in the future:</p> <ul> <li>WebSocket connects successfully (<code>onopen</code> fired, <code>self.opened</code> is <code>True</code>).</li> <li>Messages are visible in the browser's DevTools Network tab.</li> <li>Python's <code>on_message</code> never runs; <code>self.inbox</code> stays empty.</li> <li>No exceptions anywhere.</li> </ul> <p>The cause is almost always a missing strong reference to the callback.</p>"},{"location":"net/12-platform-abstraction-helpers/","title":"Platform Abstraction Helpers","text":""},{"location":"net/12-platform-abstraction-helpers/#12-platform-abstraction-helpers","title":"12. Platform Abstraction Helpers","text":"<p>Three module-level functions provide a platform-agnostic seam for runtime differences between desktop and browser:</p> <pre><code>def should_exit() -&gt; bool: ...   # check if the app should quit\ndef sleep0() -&gt; Awaitable[None]: ...  # yield for one event loop tick\ndef run_task(coro) -&gt; Task[None]: ... # schedule a background coroutine\n</code></pre> <p>They are defined with asyncio defaults, then replaced inside <code>if _IS_BROWSER:</code> with <code>aio</code> implementations:</p> <pre><code>def should_exit() -&gt; bool:\n    return False                          # desktop: never exits via this flag\n\ndef should_exit() -&gt; bool:               # browser override\n    return aio.exit                       # browser: pygbag sets this on shutdown\n</code></pre> <p>Both versions keep identical signatures so call sites remain typed uniformly.</p> <p>Important: <code>_IS_BROWSER</code> is runtime-only, so static analysis sees both branches. Keep signatures aligned and keep behavior minimal in these helpers.</p>"},{"location":"net/13-the-gameclient-api-and-packets-py/","title":"The GameNetClient API and packets.py","text":""},{"location":"net/13-the-gameclient-api-and-packets-py/#13-the-gamenetclient-api-and-packetspy","title":"13. The <code>GameNetClient</code> API and <code>packets.py</code>","text":"<p><code>GameNetClient</code> is a thin wrapper around <code>Transport</code>. The transport is protocol agnostic; packet encoding/decoding lives in demo-level packet modules.</p>"},{"location":"net/13-the-gameclient-api-and-packets-py/#api-surface","title":"API surface","text":"<pre><code>client = GameNetClient(host=\"ws://localhost:8765\")\nawait client.connect()          # opens the transport connection\n\nclient.send(data: str | bytes)  # send raw payload\nclient.recv() -&gt; bytes | None   # pop the next message, or None if inbox empty\nawait client.run()              # yield each frame; use as a background task\nclient.close()                  # close the connection\n</code></pre> <p><code>client.connected</code> is <code>True</code> after <code>connect()</code> returns successfully.</p>"},{"location":"net/13-the-gameclient-api-and-packets-py/#the-packetspy-pattern","title":"The <code>packets.py</code> pattern","text":"<p>In multiplayerv2, packets are frozen dataclasses with <code>encode() -&gt; str</code> and a top-level <code>decode(data: bytes) -&gt; Packet | None</code> dispatcher.</p> <pre><code># demo_projects/multiplayerv2/shared/packets.py\n@dataclass(frozen=True)\nclass Chat:\n    sender_id: UUID\n    text: str\n\n    def encode(self) -&gt; str:\n        ...\n</code></pre> <p>Game code uses the two together:</p> <pre><code># Sending\nclient.send(Chat(sender_id=self.my_id, text=self.input_buffer).encode())\n\n# Receiving \u2014 typically drain inbox each frame\nraw = client.recv()\nif raw is not None:\n    pkt = decode(raw)\n    if isinstance(pkt, Chat):\n        self.message_log.append(pkt.text)\n</code></pre> <p><code>recv()</code> returns <code>None</code> when the inbox is empty. Packet decoding is expected to be resilient and return <code>None</code> for malformed/unknown payloads.</p>"},{"location":"net/13-the-gameclient-api-and-packets-py/#why-recv-returns-bytes","title":"Why <code>recv()</code> returns <code>bytes</code>","text":"<p><code>Transport.inbox</code> stores bytes from all paths (desktop and browser), so callers can decode with one code path regardless of transport backend.</p> <p><code>send()</code> accepts both <code>str</code> and <code>bytes</code>.  The multiplayerv2 demo intentionally uses base64-encoded <code>str</code> packets for protocol portability and easy logging, not because the transport requires string-only payloads.</p>"},{"location":"net/13-the-gameclient-api-and-packets-py/#background-loop","title":"Background loop","text":"<pre><code>asyncio.create_task(client.run())\n</code></pre> <p><code>run()</code> yields once per loop tick via <code>sleep0()</code> and exits when <code>should_exit()</code> signals shutdown. It does not process messages; poll <code>recv()</code> in your game loop.</p>"},{"location":"net/14-the-asyncio-hijack/","title":"The asyncio Hijack","text":""},{"location":"net/14-the-asyncio-hijack/#14-the-asyncio-hijack","title":"14. The <code>asyncio</code> Hijack","text":"<p>This is subtle and important to understand when debugging unexpected behaviour.</p> <p>Inside the WASM runtime, <code>aio/__init__.py</code> ends with:</p> <pre><code>sys.modules[\"asyncio\"] = __import__(__name__)\n</code></pre> <p>After this executes, <code>import asyncio</code> anywhere in the codebase gives you <code>aio</code>, not the standard library asyncio.  This means:</p> <ul> <li><code>asyncio.sleep</code> is <code>aio.sleep</code> \u2014 same behaviour, different implementation.</li> <li><code>asyncio.create_task</code> is <code>aio.create_task</code> \u2014 tracks tasks for cancellation.</li> <li><code>asyncio.run</code> is <code>aio.run</code> \u2014 plugs into the frame scheduler, not blocking.</li> </ul> <p>Consequence: do not rely on <code>asyncio.run()</code> doing a blocking loop in the browser.  It registers the coroutine and returns.  The frame scheduler handles advancing it.</p> <p>Debugging tip: if you see <code>asyncio.something</code> behaving strangely in the browser, check whether <code>aio</code> overrides it.  The source is at:</p> <pre><code>pygbag/support/cross/aio/__init__.py\n</code></pre> <p>in your uv cache at <code>.cache/uv/archive-v0/&lt;hash&gt;/pygbag/</code>.</p>"},{"location":"net/15-data-flow-reference/","title":"Data Flow Reference","text":""},{"location":"net/15-data-flow-reference/#15-data-flow-reference","title":"15. Data Flow Reference","text":""},{"location":"net/15-data-flow-reference/#browser-js-websocket-path","title":"Browser, JS WebSocket path","text":"<pre><code>Game code calls client.send(Packet(...).encode())\n    \u2192 transport.send(str)\n    \u2192 self._js_ws.send(str)          \u2190 sync, JS handles async delivery\n    \u2192 browser WebSocket API\n    \u2192 server\n\nServer sends data\n    \u2192 browser WebSocket API\n    \u2192 on_message(event) callback\n    \u2192 event.data: str or ArrayBuffer\n    \u2192 str: inbox.append(str.encode(\"utf-8\"))\n    \u2192 ArrayBuffer: convert via Uint8Array helper \u2192 inbox.append(bytes)\n\nGame code calls client.recv() in step()\n    \u2192 transport.recv()\n    \u2192 inbox.pop(0)                    \u2190 bytes returned to caller\n    \u2192 caller: decode(bytes) \u2192 Packet | None\n</code></pre>"},{"location":"net/15-data-flow-reference/#browser-socket-fallback-path","title":"Browser, socket fallback path","text":"<pre><code>Game code calls client.send(Packet(...).encode())\n    \u2192 transport.send(str)\n    \u2192 raw = str.encode(\"utf-8\")\n    \u2192 self.sock.send(raw)            \u2190 returns int (bytes written)\n    \u2192 WASM socket bridge / pygbag proxy path\n\nServer sends data\n    \u2192 TCP from server\n    \u2192 pygbag proxy (wraps in WS frame)\n    \u2192 JS WebSocket frame\n    \u2192 WASM socket bridge\n    \u2192 _reader_browser() coroutine\n        \u2192 select.select polls self.sock\n        \u2192 self.sock.recv(4096)\n        \u2192 self.inbox.append(data)\n\nGame code calls client.recv() in step()\n    \u2192 transport.recv()\n    \u2192 inbox.pop(0)                    \u2190 bytes returned to caller\n    \u2192 caller: decode(bytes) \u2192 Packet | None\n</code></pre>"},{"location":"net/15-data-flow-reference/#desktop-websockets-path","title":"Desktop, websockets path","text":"<pre><code>Game code calls client.send(Packet(...).encode())\n    \u2192 transport.send(str)\n    \u2192 run_task(self._desktop_ws.send(str))   \u2190 schedules async coroutine\n    \u2192 asyncio event loop delivers bytes to server\n\nServer sends data\n    \u2192 asyncio event loop receives bytes\n    \u2192 _reader_desktop() coroutine\n        \u2192 async for msg in self._desktop_ws:\n        \u2192 bytes: inbox.append(msg)\n        \u2192 str: inbox.append(msg.encode(\"utf-8\"))\n\nGame code calls client.recv() in step()\n    \u2192 transport.recv()\n    \u2192 inbox.pop(0)                    \u2190 bytes returned to caller\n    \u2192 caller: decode(bytes) \u2192 Packet | None\n</code></pre>"},{"location":"net/16-known-gotchas/","title":"Known Gotchas","text":""},{"location":"net/16-known-gotchas/#16-known-gotchas","title":"16. Known Gotchas","text":""},{"location":"net/16-known-gotchas/#1-socketsend-returns-int-not-none","title":"1. <code>socket.send()</code> returns <code>int</code>, not <code>None</code>","text":"<p><code>socket.send(data)</code> returns the number of bytes actually written.  For small messages this is always <code>len(data)</code>, but you should not assume it.  The return value must be captured (<code>_ = self.sock.send(...)</code>) to avoid <code>reportUnusedCallResult</code> warnings, and in production code you should verify that all bytes were sent.</p>"},{"location":"net/16-known-gotchas/#2-socket-fallback-may-yield-partialcoalesced-chunks","title":"2. Socket fallback may yield partial/coalesced chunks","text":"<p>The socket fallback reader appends whatever <code>sock.recv(4096)</code> returns. Depending on transport conditions, a chunk can represent partial, whole, or multiple application packets. Keep packet decoding resilient.</p>"},{"location":"net/16-known-gotchas/#3-on_message-fires-on-the-js-event-loop-thread","title":"3. <code>on_message</code> fires on the JS event loop thread","text":"<p>In a real browser runtime, the JS event loop and the Python event loop run in the same Web Worker.  <code>on_message</code> is called synchronously when the browser delivers the WebSocket message.  This means it runs between Python event loop ticks, not inside a coroutine.  Do not <code>await</code> anything inside <code>on_message</code>, <code>on_open</code>, <code>on_error</code>, or <code>on_close</code>.</p>"},{"location":"net/16-known-gotchas/#4-aioexit-is-set-by-pygbag-not-by-your-code","title":"4. <code>aio.exit</code> is set by pygbag, not by your code","text":"<p>Do not set <code>aio.exit = True</code> yourself unless you intend to stop the entire pygbag runtime.  Use the <code>should_exit()</code> helper as a read-only check only.</p>"},{"location":"net/16-known-gotchas/#5-stubs-reflect-a-snapshot-in-time","title":"5. Stubs reflect a snapshot in time","text":"<p>The stubs in <code>ajishio/typings</code> were written against current pygbag/runtime behavior. If pygbag internals change, stubs can drift. When something type-checks correctly but fails at runtime, check whether the stub is outdated.</p>"},{"location":"net/16-known-gotchas/#6-jseval-exceptions-are-opaque","title":"6. <code>js.eval()</code> exceptions are opaque","text":"<p>If the JavaScript expression passed to <code>js.eval()</code> throws, the error arrives as a Python <code>Exception</code> but with limited information.  Wrap <code>js.eval()</code> calls in <code>try/except</code> and log the exception type and message for debugging.</p>"},{"location":"net/16-known-gotchas/#7-cast-is-a-lie-use-it-carefully","title":"7. <code>cast()</code> is a lie \u2014 use it carefully","text":"<p><code>cast(SomeType, value)</code> tells the type checker that <code>value</code> is <code>SomeType</code> but does absolutely nothing at runtime.  If you <code>cast</code> incorrectly, the bug will only appear at runtime, not during type checking.  Only use <code>cast</code> when you have verified the actual runtime type via <code>hasattr</code>, <code>isinstance</code>, or a logical guarantee (like <code>if hasattr(js, \"WebSocket\"):</code> before casting to <code>js.WebSocket</code>).</p>"},{"location":"net/16-known-gotchas/#8-js-callback-garbage-collection-the-silent-on_message-trap","title":"8. JS callback garbage collection (the silent <code>on_message</code> trap)","text":"<p>If you assign Python functions to <code>js.WebSocket</code> event properties and then messages stop arriving silently \u2014 even though DevTools shows them \u2014 the cause is almost certainly garbage collection.</p> <p>When you do <code>self._js_ws.onmessage = on_message</code>, JavaScript receives a proxy wrapper around the Python callable, not the callable itself.  If Python drops its reference to the original function, the GC reclaims it. JavaScript still holds the proxy, but calling it does nothing.</p> <p>Fix: store strong references to every callback on the <code>Transport</code> instance:</p> <pre><code>self._on_open    = on_open\nself._on_message = on_message\nself._on_error   = on_error\nself._on_close   = on_close\n\nself._js_ws.onopen    = self._on_open\nself._js_ws.onmessage = self._on_message\n</code></pre> <p>As long as <code>Transport</code> is alive, the callbacks cannot be collected. See \u00a711 for the full explanation and type annotations.</p>"},{"location":"net/16-known-gotchas/#9-the-simulator-does-not-emulate-everything","title":"9. The simulator does not emulate everything","text":"<p>The desktop pygbag simulator runs your code in a desktop Python process with a simulated browser-like environment.  But it does not run a real browser, so:</p> <ul> <li><code>js.WebSocket</code> may not be available or may behave differently.</li> <li>The port +20000 proxy is handled differently.</li> <li>Performance characteristics are completely different.</li> </ul> <p>Always validate in a real browser before shipping.</p>"},{"location":"net/16-known-gotchas/#10-url-requirements-differ-across-paths","title":"10. URL requirements differ across paths","text":"<p>The fallback <code>_parse_url</code> requires <code>host:port</code> after scheme stripping. A URL such as <code>wss://host/path</code> without explicit port cannot be parsed by fallback. The demo uses JS WebSocket as primary path for such URLs.</p>"},{"location":"net/17-extending-this-into-the-engine/","title":"Extending This Into the Engine","text":""},{"location":"net/17-extending-this-into-the-engine/#17-extending-this-into-the-engine","title":"17. Extending This Into the Engine","text":"<p>This networking layer already lives in the engine (<code>ajishio/src/ajishio/net.py</code>) and is exposed as <code>aj.GameNetClient</code>. This page focuses on future evolution.</p>"},{"location":"net/17-extending-this-into-the-engine/#stub-maintenance","title":"Stub maintenance","text":"<p>Keep stubs in <code>ajishio/typings</code> in sync with runtime usage in <code>net.py</code>. Current workspace configuration already points basedpyright there:</p> <pre><code>[tool.basedpyright]\nstubPath = \"ajishio/typings\"\n</code></pre>"},{"location":"net/17-extending-this-into-the-engine/#engine-integration-points","title":"Engine integration points","text":"<p>The three platform helpers (<code>should_exit</code>, <code>sleep0</code>, <code>run_task</code>) and the <code>_IS_BROWSER</code> flag are the natural seam.  The engine already has a game loop; the browser version needs to yield via <code>aio.sleep(0)</code> on every frame.  The async game loop in <code>aj.game_start_async()</code> is already set up for this.</p>"},{"location":"net/17-extending-this-into-the-engine/#optional-transport-abstraction","title":"Optional transport abstraction","text":"<p>Consider extracting a <code>Protocol</code> that <code>Transport</code> (or its successors) implement.  This would let the engine define the interface without depending on the concrete implementation:</p> <pre><code>class ITransport(Protocol):\n    async def connect(self) -&gt; None: ...\n    def send(self, data: str | bytes) -&gt; None: ...\n    def recv(self) -&gt; bytes | None: ...\n    def close(self) -&gt; None: ...\n</code></pre>"},{"location":"net/17-extending-this-into-the-engine/#gamenetclient-as-an-engine-service","title":"<code>GameNetClient</code> as an engine service","text":"<p><code>GameNetClient</code> is a service with a lifecycle (<code>connect</code>, <code>poll</code>, <code>close</code>). It maps naturally onto an <code>aj.GameObject</code> subclass (as demonstrated in <code>main.py</code>) or onto an engine-level service registry if you build one.</p>"},{"location":"net/17-extending-this-into-the-engine/#framing-and-buffering-improvements","title":"Framing and buffering improvements","text":"<p>If you need strict packet boundaries in fallback mode, add optional framing at the protocol layer (for example length-prefix framing or newline-delimited messages) and decode incrementally.</p>"}]}